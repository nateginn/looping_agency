# Risk Register — Loop Agency

Canonical record of review findings, their severity, and their resolution. A phase cannot be marked complete while a High finding is Open, unless the human records a written risk acceptance here.

| ID | Source | Severity | Finding | Status | Resolution / Acceptance |
|----|--------|----------|---------|--------|--------------------------|
| R1 | Codex plan review R1 #12 | Low | Solo-desktop scheduling lacks dedicated service account | Accepted by design | Compensating controls: out-of-band watchdog + staleness surfacing. Service account documented as optional hardening. Human sign-off pending with plan approval. |
| R2 | Codex plan review R1 #16 | Low | 2-round plan-review cap may leave findings unresolved | Mitigated | This register + high-severity block rule + human tie-break on deadlock. Cap is user-mandated. |
| R3 | Codex plan review R2 (all 5) | Medium | Stale locks, Tier-1 apply boundary, .env storage, missing watchdog, non-canonical path checks | Resolved in plan | All five incorporated into AgentColabPlan.md Round 2; Codex has not re-verified the final edits (round cap reached). Verify during Phase 1 implementation review. |
| R4 | Plan (Phase 4) | Medium | Meta/Google Ads API onboarding may stall; live spend controls unverified until then | Open | Ads loop stays propose-only until daily ceilings verified live; CSV-export fallback defined. Revisit at Phase 4. |
| R5 | Plan (metrics) | Low | Qualitative guardrail rubrics (content quality, brand voice) uncalibrated until real content flows | Open | Rubrics always human-approved; expect revisions in Phase 2/3. |
| R6 | User (2026-07-15, pre-Phase-1) | High | The user's website project (in the `Dev` directory, separate repo from this workspace) deploys to production automatically on any push — there is no staging gate. Any `git push` to that repo is therefore a live, public, irreversible-by-default action, not a Tier-1 preview. | Accepted by design | Classified as **Tier 2 (public/paid, human-only)** for any push, merge, or remote-tracking-branch update — never a loop's `applied` transition. A local working-tree branch/commit is not itself Tier 2, but pushing it always is, unconditionally. Phase 1 does not touch this repo, any real credentials, or any live API; the runner's per-project namespace check (resolved canonical paths) must confirm no run can write into that repo's working directory even if a future project onboards it. Revisit scope/branch-protection options only if/when this site is ever onboarded as a project — not assumed in Phase 1. |
| R7 | Codex read-only adversarial review, 2026-07-15 (gpt-5.4, sandbox read-only), `VERDICT: REVISE`, 4 findings | Medium | (1) `apply.mjs` would apply an approved Tier-1 proposal even for a `propose-only` loop, with no spec-level check tying `applied` to an explicit apply-enabled mode. (2) `run-loop.mjs`'s stale-lock TTL was hardcoded to 60 minutes regardless of the loop's own `max_run_duration_minutes`, so a loop with e.g. a 5-minute expected duration could have a stale lock block runs for up to an hour. (3) Tier-1 applied-markers were written into `pending/` alongside real proposal files, risking a marker being misread as proposal state by `listProposals`. (4) `review-pending.mjs --approve` could move a proposal straight from `draft` to `approved`, skipping the `reviewed` state the state-machine names in `CLAUDE.md`. | Resolved (3 fixed, 1 kept-by-design) | (1) `apply.mjs` now refuses a Tier-1 apply unless `spec.approval_mode === "tier1-enabled"`; `_demo`'s spec sets this explicitly (documented inline as a demo-only exception). (2) Stale-lock TTL now reads `max_run_duration_minutes` from the loop's own spec (clamped, best-effort) instead of a fixed 60 minutes. (3) Applied-markers now write to a new `applied/` directory, never `pending/`; `listProposals` no longer needs to filter them out. (4) Kept by design — the plan's wording arguably permits `draft → approved` directly — but `review-pending.mjs` gained a `--review` flag so the full `draft → reviewed → approved` path is reachable when a human wants it. All four fixes verified via `tools/tests/phase1-exit-criteria.mjs` (`testProposeOnlyRefusesTier1Apply`, `testStaleLockTtlComesFromSpec`, plus existing marker-placement checks) and via a clean two-consecutive-run proof against `_demo` on 2026-07-16. **Note: this row describes the Node.js implementation, since replaced by R8 — the underlying findings and fixes are still historically accurate and were carried forward into the Python port.** |
| R8 | User correction, 2026-07-16 | High | The entire Phase 1 framework (`tools/*.mjs`) was implemented in Node.js. The user states they told an earlier session to use Python and believed that was agreed; the written record (`looping_agency_plan.md:64`, `AgentColabPlan.md:18`, `PLAN-REVIEW-LOG.md:40,65`) shows Node was chosen from the first draft onward, justified by this workstation's AVG TLS-interception issue ([[avg-tls-interception]] memory) — a justification that doesn't hold up, since that issue has a one-line fix (`pip install truststore` + `truststore.inject_into_ssl()`, already confirmed working on this machine). No document shows the user's Python instruction being captured or the switch to Node being re-confirmed with them. An entire Phase 1 framework was built, and this session continued extending it (real GSC/DataForSEO connector implementations, `watchdog.mjs`, intake checklist) before the mismatch surfaced. Independently, the user asked Codex for a second opinion; Codex's read-only assessment also recommended Python for this specific codebase (small local orchestration/CLI tooling, not browser/Node-ecosystem-dependent) and to rewrite now rather than later, while the repo is still small. | Resolved | Full rewrite of `tools/` from Node.js (`.mjs`) to Python (`.py`), same day, all files: `lib/paths.py`, `lib/redact.py`, `lib/lock.py` (cross-platform PID-liveness check via `ctypes`/`OpenProcess` on Windows, since `os.kill(pid, 0)` actually terminates a process on Windows unlike Node's `process.kill(pid, 0)`), `spec_validate.py`, `snapshot.py`, `mock_metrics.py`, `gsc.py`, `dataforseo.py` (stdlib `urllib.request`, no new dependency), `run_loop.py`, `review_pending.py`, `apply.py`, `watchdog.py`, and the full `tools/tests/phase1_exit_criteria.py` integration suite — all 32 exit-criteria checks plus every module's `--verify` self-test pass identically to the Node version. All `.mjs` files, `package.json`, and `package-lock.json` removed; `node_modules/` was already gitignored. `_demo`'s run history was regenerated end-to-end via the Python engine (same two-consecutive-run proof: run 2 evaluates the applied proposal as a verified winner). All Node references in `CLAUDE.md`, the three skills, and `templates/` were updated to the Python equivalents. A standing rule was saved to the operator's persistent memory (`language-choice-approval` memory, referenced from `CLAUDE.md`): never pick a non-Python implementation language for this user's projects without explicit prior approval, regardless of a plausible-sounding technical reason. |
| R9 | Phase 2 Step 6 execution, 2026-07-16 to 2026-07-18 | Low | Phase 2 kickoff for the first real project (`art`) was explicitly user-authorized 2026-07-16. The live smoke test (`tools/smoke_test.py art`) and first real run (`tools/run_loop.py art seo`) both initially returned 0 rows/0 proposals on 2026-07-16 — Search Console had not yet finished processing data for the newly-verified `sc-domain:acceleratedrehabtherapy.com` property. Both were retried 2026-07-18 and succeeded with real data (177-row smoke test; 451-sample-size run producing 3 draft proposals). No connector, credential, or tooling defect — a normal data-availability lag on a freshly-verified GSC property. | Resolved | Retried after the property finished processing; no code change needed. `HANDOFF.md` and `PHASE2_READINESS_CHECKLIST.md` §5/§6 updated to reflect both the 0-row and real-data runs. `art` remains `propose-only` with all `allowed_actions` `manual_approval_only: true`; the 3 draft proposals from the 2026-07-18 run await Nate's `/review-pending` decision — no Tier-1 apply path exists for this project regardless of that decision. |
| R10 | Amends R6 — Plan (`PLAN.md` Phase 5), implemented and independently reviewed 2026-07-29/30 | Low | R6's original wording classified "any push, merge, or remote-tracking-branch update" as Tier 2/human-only. Phase 5 gave `apply.py` a real, tooling-performed remote-tracking-ref update: before creating a worktree it runs `git fetch origin +refs/heads/main:refs/remotes/origin/main` and refuses if the local `refs/heads/main` no longer matches `refs/remotes/origin/main` (baseline drift). Left as originally worded, R6 would read that fetch as already-forbidden Tier 2 — it isn't, and the distinction needs to be explicit rather than inferred. | Resolved | R6's Tier-2 boundary is **narrowed to what it always meant in practice: writes to the actual remote** — push, merge, deployment, and rollback. **Read-only** `git fetch` that only updates this checkout's own local remote-tracking ref cache (never opens a connection capable of writing) is Tier 0/tooling-permitted, and is now required before every Tier-1 apply attempt, refusing safely on drift, a missing ref, or a fetch failure. Local, unpushed `git worktree`/branch/commit creation remains Tier 1/tooling-permitted only for an approved, non-`manual_approval_only` proposal on a loop that has explicitly opted in (unchanged from R6/Phase 3). Push, merge, deployment, and rollback remain Tier 2/human-only, unconditionally — no code exists to perform any of them (`publish.py` is unbuilt, Phase 6). `art` stays `approval_mode: propose-only` with `manual_approval_only: true` on every action, so neither the fetch nor the worktree path fires for it today. Verified via `tools/tests/phase1_exit_criteria.py` (baseline-drift refusal, fully-qualified-ref usage, base-SHA persistence/backfill, ancestry-mismatch refusal, no-temp-dir-leak-on-refusal — 133/133) and `tools/lib/artwebsite_seo.py --verify`. |
| R11 | Plan (`PLAN.md` Phase 6), implemented and offline-tested 2026-07-30 | Medium | `tools/publish.py` was built: for an `implemented` proposal with a recorded human approval, it re-verifies commit ancestry, validates the diff from the commit's own git objects (exactly one changed file, blob contains the approved `new_value`), confirms the local branch hasn't moved, checks `.github/workflows/deploy.yml`'s trigger shape at the base commit, validates `origin`'s push URL, hard-refuses unless GitHub branch protection on `main` is independently confirmed live, then pushes to `seo/<id>`, opens a PR (`lib/github_compare.py`'s new `create_pull_request()`), and requests auto-merge (`enable_auto_merge()`, GraphQL). This is real push/PR/merge-request capability — the exact Tier-2 boundary R6/R10 draw the line at — landing in the codebase for the first time, so the register needs an honest entry even though nothing is activated. | Open — code shipped, live verification and activation both outstanding | **What is true today:** no project's `project.md`/`spec.md` references `publish.py`, no `<project>-github-token` credential alias has been stored, and no skill or automated path calls it — it is dead code from the perspective of every existing loop, callable only by a human running it directly with explicit `--owner/--repo` or a `LIVE_COMPARE_TARGETS` entry (currently only `art`, itself still `propose-only`/`manual_approval_only: true`, so `apply.py` never produces an `implemented` proposal for it to act on). **What is NOT yet true:** `PLAN.md` Phase 0 (separate automation identity, branch protection with required review + no-bypass + push restrictions, and the live fail-closed test matrix in Phase 0c) has not been performed against the real `nateginn/artwebsite` repo or a real GitHub credential — `publish.py`'s `verify_branch_protection()` hard-refuses on a missing/misconfigured/unqueryable protection response, so an unconfigured `main` fails closed by construction, but that logic itself has only been exercised against fake and local-fixture HTTP/git responses, never a real GitHub API response shape. Before any real use: (1) complete Phase 0 with the real credential per `PLAN.md`'s fail-closed matrix, (2) store a `<project>-github-token` alias for a separate (non-Nate) automation identity, (3) explicitly wire a project's config to call `publish.py` — none of which this session performed, per explicit user instruction. Verified offline via `tools/publish.py --verify` (32 checks, every refusal + success path with fake git/HTTP) and `tools/tests/phase1_exit_criteria.py` (5 new real-git-repo integration tests: end-to-end push+PR+auto-merge against a local bare origin, plus real `git diff-tree`/`.github/workflows/deploy.yml`/branch-protection/remote-URL refusals — 144/144 total). |
| R12 | Phase 6a implementation, offline-tested 2026-07-30 | Medium | Post-deployment health verification and rollback orchestration was added without activating production actions. | Open — orchestration implemented; production integration intentionally absent | `tools/lib/deploy_health.py` provides bounded DNS/TLS/HTTP/canonical/robots/representative-page checks and advisory commit evidence. `tools/deploy_verify.py` persists new and previous SHAs, waits non-blockingly for the stabilization window, records health/rollback outcomes, redacts every write, and escalates on rollback failure. Verified by 31 + 42 module checks and the full 153/153 suite. The default redeploy hook refuses rather than pushing or deploying; no project callback/scheduler is wired; no credentials or live deployment were used. `art` remains propose-only/manual-approval-only. |
| R13 | Phase 7 (`PLAN-PHASE7-CODEX-REVIEW.md`, six rounds of Codex adversarial plan review — `PLAN-REVIEW-LOG-PHASE7.md`), implemented and offline-tested 2026-08-03, built while the user was away from keyboard per his explicit prior authorization to use Codex review in place of questions that would otherwise go to him | Medium | A second, independent Codex review stage plus eligibility-gated **automatic local-commit implementation** (still the pre-existing Tier-1 boundary — never push/publish/deploy) was added for low-risk SEO proposals, landing real automatic-implementation capability in the codebase for the first time without any individual per-proposal human sign-off, for whichever loop explicitly opts in. Also: a new durable, event-sourced (for the new review subsystem) / best-effort (for the pre-existing state machine) event history, and an MMC briefing section reading it. | Superseded by R14 (activated 2026-08-03) | **Historical — describes the pre-activation state.** See R14 for what changed. Verified offline via `tools/tests/phase1_exit_criteria.py` (191/191), every new/changed module's own `--verify` self-test, and MMC's `looping_events.py`/`advance_looping_cursor.py` self-tests. No live SEO run, live GitHub call, live Telegram send, or touch to any existing pending proposal occurred during this original build. |
| R14 | User (2026-08-03), explicit activation request in the current session | Medium | `art/seo`'s `spec.md` was switched from `propose-only`/all-manual to `approval_mode: tier1-enabled` + `auto_implementation_enabled: true`, with `manual_approval_only: false` on `title-tag-rewrite` and `meta-description-rewrite` only (`internal-link-addition` stays `manual_approval_only: true`, so it still can never auto-implement — no HTML-mutation engine exists). This is the first real project where the Phase 7 auto-implementation path (Codex two-round adjudication → local, unpushed git commit) is actually reachable. | Open — activated; scheduler limitation confirmed, review stays on-demand | **What is true now:** the three gates are live for `art/seo`'s two low-risk action types; `apply.py` re-reads `spec.md` fresh at commit time (twice — once at eligibility check, once immediately pre-`git commit`), so flipping any gate back off genuinely disables the path. **Scheduler decision, made this session, not deferred:** this environment's two available scheduling primitives were inspected and found unsuitable for a zero-touch `/codex-seo-review` — `RemoteTrigger` (cloud-hosted scheduled routines) has no access to this local repo, local git, Windows Credential Manager, or this machine's local `codex login` session; `CronCreate` is explicitly session-only (in-memory, dies when the invoking conversation ends, 7-day auto-expiry cap, only fires while a session is idle) and therefore not durable unattended infrastructure. Windows Task Scheduler → bare `python.exe` still cannot reach `codex exec` at all (confirmed, unchanged from R13). Conclusion: `/codex-seo-review` remains invoked on demand only — this is a considered decision with a documented reason, not an unexamined gap. Plain-Python `run_loop.py`/`watchdog.py` jobs (no Codex dependency) were registered via `schtasks` per `PHASE3-SCHEDULING.md` — those are safe for OS-level scheduling since they never touch Codex. Boundaries unchanged and re-verified this session: no push/publish/deploy/merge/rollback tooling was touched or invoked; the two pre-existing draft proposals from the 2026-08-03T14:37 run were left untouched; only newly created proposals were used for the first activation/review test. |
| R15 | Operational audit of the live `art/seo` schedule, 2026-08-10 (user-requested review of PLAN.md completion + running state) | Medium | Four defects in how the loop actually runs unattended, none visible from the code alone. (1) **Any** connector error aborted the entire run: the 2026-08-10 weekly `full` run died on a transient Google PSI 500 (`Lighthouse returned error`) raised before GSC was even called, so the one run per week that generates proposals produced no snapshot, no evaluation and no proposals — a third-party flake cost a whole cycle, with no retry and no per-connector degradation. (2) `LoopAgency-Art-SEO-DailyRank` was scheduled at 06:00, the same minute as both the Monday full run and the Thursday technical run; all three contend for the same per-loop `run.lock`, so the daily rank check was refused outright on Mondays and Thursdays (confirmed in `lock-refusals.log` 2026-08-06 and 2026-08-10) — 2 of 7 daily checks silently lost every week. (3) `tools/watchdog.py` exited non-zero every single day because the `_demo` offline fixture, which is on no schedule, was 26+ days "stale" — a watchdog that always alerts is a watchdog nobody reads. (4) All four scheduled tasks embedded `>> <log> 2>&1` inside `schtasks /TR`; Task Scheduler launches the executable directly rather than through a shell, so the redirection was passed to `python.exe` as literal arguments and **no log file was ever created** — the watchdog, whose only output is stdout, was reporting into nothing. | Resolved 2026-08-10 | (1) `connector_registry.py` gained an explicit `critical` flag with the rationale recorded in-file. Critical = the `search_analytics` contributors (`gsc`, `dataforseo`, `mock`); their failure still aborts the run as `partial-failure` with no snapshot, because degrading them would mean scoring proposals against a carried-forward stale section — the exact false-win route PLAN.md Phase 1 closed. Enrichment connectors (`pagespeed`, `gsc-indexation`, `dataforseo-local-rank`, `dataforseo-backlinks`) now degrade instead: failure recorded as a failed `tool_call`, listed in a new `degraded_connectors` field, surfaced in report.md's Needs Attention and in the `memory.md` line, run status `degraded` (distinct from `partial-failure`, which still means "nothing was produced"), and the run's evaluation/proposal steps continue. An unknown connector name is treated as critical, never silently degraded. `pagespeed.py` retries transient 5xx and transport failures (3 attempts, 2s/5s backoff) but never a 4xx, so 403/429 quota answers are not re-burned. `_technical_health_section()` seeds from the previous snapshot so a partial refresh of the pagespeed/indexation shared section carries the other half forward instead of blanking it to `[]`. (2) Daily rank moved to 07:00 (observed full runs take 2–4 min; lock TTL ceiling is 30 min). (3) `watchdog.py` now reads `loops_enabled` from `project.md` — the field every spec's `stop_condition` already named but nothing read — and reports a non-enabled loop as `not-enabled` instead of alerting; `_demo`'s list was emptied. A missing/unparseable `project.md` still means "monitored", so the change can never create a blind spot. `run_loop.py` deliberately does **not** read the field, so `_demo` stays runnable by hand. (4) All four tasks re-registered against a new version-controlled wrapper, `tools/scheduled/task.cmd`, which resolves the workspace root from its own path, pins the `.venv` interpreter, appends stdout+stderr to a rotating `logs/<name>.log`, and exits with the child's code; the watchdog also moved 06:15 → 07:30 so it assesses the day that just ran rather than the previous one. Verified: 211/211 in `tools/tests/phase1_exit_criteria.py` (20 new checks — degradation end-to-end, critical-still-aborts, partial technical-health merge, PSI retry/give-up/no-4xx-retry), every module `--verify` self-test, argument forwarding through the wrapper proved exactly (a dropped `--run-name` would have silently promoted the daily job to a full paid run), and `LoopAgency-Watchdog` executed live via Task Scheduler writing a real log and exiting 0. No boundary moved: no push/publish/deploy/merge path was touched, no credential was read, and no live SEO API call was made. |
| R16 | User direction, 2026-08-10, restating and expanding the 2026-07-30 operating-model choice | High | Nate confirmed **Path B** as the selected model for getting an approved SEO change live: the loop pushes the approved commit **directly to the deployment branch** of `D:\Dev\artwebsite` (which auto-deploys with no staging gate), then verifies the live site, rolls back on failure, and reports through MMC's daily briefing. His stated requirements: (1) no per-change human approval — he is not the gate; (2) notification when a change is made, via the MMC briefing rather than a separate channel; (3) **positive** confirmation that the change did not damage the site, not merely absence of an alarm; (4) explicit notification with troubleshooting detail if a rollback fired. This direction directly contradicts R6/R10 as currently written, which classify **every** push to that repo as Tier 2 / human-only / unconditional, precisely because it auto-deploys. It also strands `tools/publish.py` (816 lines) and the branch-protection/PR/auto-merge block of `tools/lib/github_compare.py`, which implement the opposite model (Path A) and refuse a `main`/`master` destination by construction. | Open — direction recorded and documentation reconciled; **no Path B capability built, no boundary moved** | **Done this session (documentation and labelling only):** `PLAN.md` gained a "SELECTED OPERATING MODEL" section at the top stating Path B, Nate's four requirements verbatim, an honest what-exists/what-doesn't table, the gate list Path B must clear, and a plain statement that Path B has **no** control that survives a compromised local agent (it is an accountability and recovery model, not a security boundary — the same position this project already takes on `publish.py`'s guards). Path-A-only sections are marked `[HISTORICAL — PATH A]` rather than deleted, preserving six rounds of Codex review history. `publish.py` and the Path-A block of `github_compare.py` carry prominent DORMANT BY DECISION headers explaining that they implement the unselected model and cannot be flag-flipped into Path B; `compare_commit_to_main()` is explicitly exempted, since `run_loop.py` still calls it every run to promote `implemented → applied`. `CLAUDE.md` records the selected model in its opening section. **Explicitly NOT done, and not to be inferred:** no push capability was written; nothing in this workspace pushes, merges, deploys, or rolls back; `deploy_verify.py`'s redeploy hook still refuses by default and is wired to no project; no deploy/health/rollback events are emitted to `events.jsonl`, so requirements (3) and (4) cannot be satisfied by the briefing today. **Before any Path B capability is built, R6 must be amended by a dated row of its own, with reasoning** — a direction stated in this row is not an amendment to that one. Verified unchanged this session: 211/211 in `tools/tests/phase1_exit_criteria.py`, plus `publish.py --verify` and `lib/github_compare.py --verify` still passing after the labelling edits. |
| R17 | Two independent audits, 2026-08-16 (`CURRENT-WORK.md` findings 4 and 5), confirmed against the live artifacts and against a one-shot SERP diagnostic run 2026-08-11 | High | **`tools/dataforseo.py`'s rank collection had never worked, in three independent ways at once, on a paid connector feeding a live loop — and every failure mode produced a plausible-looking number rather than an error.** (D4, critical) `pull_local_rank` built one task per target and POSTed them as a **single array**; DataForSEO's `serp/google/organic/live/advanced` endpoint executes exactly one task per request and rejects every additional one with `status_code: 40000`, `result: null`, `cost: 0`. Downstream, `result = task.get("result") or []` converted that rejection into an empty item list, which produced `organic_rank_position: null` — byte-identical to a genuine "not ranking". Across all 16 observations in history every non-null result was the first target of its batch, without exception: **10 of 12 targets were never queried at all** across weeks of daily runs, and were reported as absent. The 2026-07-26 "36 checks -> 12 checks" cost optimisation was therefore moot; only 2 tasks per run were ever billed. (D1, high) Results were matched with `target["page"] in (item.get("url") or "")` — a bare URL-substring test with **no host check** — so any competitor URL containing the configured path was recorded as the client's rank. Confirmed concretely in the 2026-08-16 snapshot: `physical therapy denver` was stored as rank 6 pointing at a competitor's `/physical-therapy/`, and `physical therapy greeley` as rank 40 pointing at another. This fed `attention_thresholds[organic_rank_position]`, so the loop could raise an alarm on a competitor's movement. (D5, high) The value stored under the name `organic_rank_position` was `rank_absolute`, which numbers **every** SERP element including ads, local-pack entries and People Also Ask blocks — so every "organic rank" ever reported was a mixed-feature position. (D6, found while fixing the above) `run_loop.py`'s `_evaluate_attention` computed its delta as `(_numeric(x) or 0)` on both sides, reading a **missing** position as rank 0 — the best rank there is — so a target moving from "no answer" to a real rank of 40 computed as a `+40` delta and fired the threshold, and a genuine drop out of the results computed as an improvement. `pull_metrics`'s SERP path carried D4/D1/D5 identically; it is enabled for no project, so it was latent rather than live. | Resolved 2026-08-16 (`dccb53a`, pushed) | New `tools/lib/serp_match.py` holds the shared matcher (`registrable_host`, `path_matches` with segment boundaries, `is_own_url`, `scan_organic`); `lib/timeseries.py` imports it instead of keeping its own copies, so the connector, the dashboard and `serp_diagnostic.py` cannot diverge on "is this ours?" again. `dataforseo.py`: **one POST per target**; tasks selected by the keyword the API echoes back in `task.data.keyword`, never by array index (a reordered or partially-failed response would otherwise attribute one query's SERP to another); every non-`20000` outcome — plus transport failure, non-2xx, missing/duplicate/mismatched task, and a `20000` carrying no `result`/`items` — recorded as an explicit **error** with an enumerated reason code, never as an empty result. Each row is `status: ok / absent / error`, where `absent` requires positive evidence of a populated SERP to be absent from, and carries `organic_results_seen` so the verdict is auditable. Position is the **counted organic index**, cross-checked against `rank_group` (which agreed on all 12 live rows; the count wins on disagreement). Non-matching results are discarded outright — no competitor host, URL or title reaches disk (`CURRENT-WORK.md` decision 4) — and `result_url` is only ever the client's own. Exactly one row per requested target is asserted post-loop. `pull_metrics` got the same fix but **fails closed** (any target failure raises), because `dataforseo` is `critical` and a partially-enriched `search_analytics` is worse than none; `dataforseo-local-rank` is enrichment and degrades per-target, raising only if every target failed. `domain` is now **required** for both inputs (`connector_registry.py` `requires`) with no fallback. **Rebaseline as code, not prose** (`PLAN-SEO-PROGRAM-INTEGRATION.md` P0.1, Codex round 10 #2): the section carries `schema_version: 2` and `_section_history` refuses to hand any pre-v2 `local_rank` section to the attention comparison — keyed on the section's own version rather than a baseline run id, because `_build_snapshot` carries un-refreshed sections forward verbatim, so a pre-fix section outlives its own run directory. D6 fixed via `_rank_of`, which returns None for an `error` row or a non-numeric position instead of coercing to 0. Report and dashboard now distinguish "not in results (of N seen)" from "NO ANSWER"; the dashboard's integrity panel is split into a corrected era and a closed pre-fix era, since its D4 claim became false the moment this landed. Verified: 225/225 in `tools/tests/phase1_exit_criteria.py` (was 211 — new checks cover the 12-POST assertion, every failure classification, the cutoff, a positive control proving the cutoff filters history rather than disabling the check, and the null-as-zero regression), every module `--verify`, and **one controlled live run** with the weekly scheduled task disabled for the duration: 12 requests, 12 rows, 11 answered / 1 error, every position resolving to the client's own host, zero non-client hosts anywhere in the snapshot, and zero historical rank alarms (counterfactually 17 prior observations would have been compared). First true measurement: 4 of 12 targets rank (Greeley physical therapy #46, work comp #21, acupuncture #35; Denver auto injury #18), 7 genuinely absent, 1 no answer. No boundary moved. MMC's briefing side (`D:\Dev\MMC` `collector/sources/looping.py`) was fixed the same day so an `error` row is not briefed as an absence — see `CURRENT-WORK.md` "MMC". |
| R18 | This register's own R14 vs `CURRENT-WORK.md` standing decision 6 vs `SEO_PROGRAM.md` Part 3A priority 0, noticed 2026-08-16 while closing R17 | Medium | **Three records disagreed about whether `art/seo`'s automation was on.** Decision 6 said "automation stays off until 'did it work?' is trustworthy" and Part 3A priority 0 said keep the loop in propose-only/manual mode, while `projects/art/loops/seo/spec.md` still carried the R14 activation (`approval_mode: tier1-enabled` + `auto_implementation_enabled: true`, `manual_approval_only: false` on the two rewrite actions). The spec is the only one of the three that any code reads, so the written intent was not in force. The underlying reason for decision 6 is unchanged and still open: `_evaluate_prior_experiments` has no "worse" outcome — anything short of a guardrail breach is filed as a verified winner, including a page that slipped four positions — so the loop's success verdict remains vacuous. | Resolved 2026-08-16 | `spec.md` set to `approval_mode: propose-only` + `auto_implementation_enabled: false`, with the reasoning recorded inline above those keys. `approval_mode` is the switch that actually closes both doors: `apply.py:216-222` reads it fresh from the spec at apply time and refuses **every** Tier-1 apply regardless of whether a human `approved` via `/review-pending` or Codex review reached `approved-for-implementation`; `auto_implementation_enabled: false` alone gates only the Codex path (`apply.py:233`) and is defence in depth. Nothing had been auto-implemented under R14: zero proposals ever reached `applied`, and `pending/` was empty at the reversal, so no already-created proposal carries a stale cached `manual_approval_only: false` (which `apply.py:223` reads from the proposal rather than the spec). Proposals are still generated — `propose-only` blocks application, not drafting. Restoring R14's settings requires an explicit recorded go/no-go and should not precede the "did it work?" backlog item. `CLAUDE.md` and `CURRENT-WORK.md` updated to match. |
