# Risk Register — Loop Agency

Canonical record of review findings, their severity, and their resolution. A phase cannot be marked complete while a High finding is Open, unless the human records a written risk acceptance here.

| ID | Source | Severity | Finding | Status | Resolution / Acceptance |
|----|--------|----------|---------|--------|--------------------------|
| R1 | Codex plan review R1 #12 | Low | Solo-desktop scheduling lacks dedicated service account | Accepted by design | Compensating controls: out-of-band watchdog + staleness surfacing. Service account documented as optional hardening. Human sign-off pending with plan approval. |
| R2 | Codex plan review R1 #16 | Low | 2-round plan-review cap may leave findings unresolved | Mitigated | This register + high-severity block rule + human tie-break on deadlock. Cap is user-mandated. |
| R3 | Codex plan review R2 (all 5) | Medium | Stale locks, Tier-1 apply boundary, .env storage, missing watchdog, non-canonical path checks | Resolved in plan | All five incorporated into AgentColabPlan.md Round 2; Codex has not re-verified the final edits (round cap reached). Verify during Phase 1 implementation review. |
| R4 | Plan (Phase 4) | Medium | Meta/Google Ads API onboarding may stall; live spend controls unverified until then | Open | Ads loop stays propose-only until daily ceilings verified live; CSV-export fallback defined. Revisit at Phase 4. |
| R5 | Plan (metrics) | Low | Qualitative guardrail rubrics (content quality, brand voice) uncalibrated until real content flows | Open | Rubrics always human-approved; expect revisions in Phase 2/3. |
| R6 | User (2026-07-15, pre-Phase-1) | High | The user's website project (in the `Dev` directory, separate repo from this workspace) deploys to production automatically on any push — there is no staging gate. Any `git push` to that repo is therefore a live, public, irreversible-by-default action, not a Tier-1 preview. | Accepted by design | Classified as **Tier 2 (public/paid, human-only)** for any push, merge, or remote-tracking-branch update — never a loop's `applied` transition. A local working-tree branch/commit is not itself Tier 2, but pushing it always is, unconditionally. Phase 1 does not touch this repo, any real credentials, or any live API; the runner's per-project namespace check (resolved canonical paths) must confirm no run can write into that repo's working directory even if a future project onboards it. Revisit scope/branch-protection options only if/when this site is ever onboarded as a project — not assumed in Phase 1. |
| R7 | Codex read-only adversarial review, 2026-07-15 (gpt-5.4, sandbox read-only), `VERDICT: REVISE`, 4 findings | Medium | (1) `apply.mjs` would apply an approved Tier-1 proposal even for a `propose-only` loop, with no spec-level check tying `applied` to an explicit apply-enabled mode. (2) `run-loop.mjs`'s stale-lock TTL was hardcoded to 60 minutes regardless of the loop's own `max_run_duration_minutes`, so a loop with e.g. a 5-minute expected duration could have a stale lock block runs for up to an hour. (3) Tier-1 applied-markers were written into `pending/` alongside real proposal files, risking a marker being misread as proposal state by `listProposals`. (4) `review-pending.mjs --approve` could move a proposal straight from `draft` to `approved`, skipping the `reviewed` state the state-machine names in `CLAUDE.md`. | Resolved (3 fixed, 1 kept-by-design) | (1) `apply.mjs` now refuses a Tier-1 apply unless `spec.approval_mode === "tier1-enabled"`; `_demo`'s spec sets this explicitly (documented inline as a demo-only exception). (2) Stale-lock TTL now reads `max_run_duration_minutes` from the loop's own spec (clamped, best-effort) instead of a fixed 60 minutes. (3) Applied-markers now write to a new `applied/` directory, never `pending/`; `listProposals` no longer needs to filter them out. (4) Kept by design — the plan's wording arguably permits `draft → approved` directly — but `review-pending.mjs` gained a `--review` flag so the full `draft → reviewed → approved` path is reachable when a human wants it. All four fixes verified via `tools/tests/phase1-exit-criteria.mjs` (`testProposeOnlyRefusesTier1Apply`, `testStaleLockTtlComesFromSpec`, plus existing marker-placement checks) and via a clean two-consecutive-run proof against `_demo` on 2026-07-16. **Note: this row describes the Node.js implementation, since replaced by R8 — the underlying findings and fixes are still historically accurate and were carried forward into the Python port.** |
| R8 | User correction, 2026-07-16 | High | The entire Phase 1 framework (`tools/*.mjs`) was implemented in Node.js. The user states they told an earlier session to use Python and believed that was agreed; the written record (`looping_agency_plan.md:64`, `AgentColabPlan.md:18`, `PLAN-REVIEW-LOG.md:40,65`) shows Node was chosen from the first draft onward, justified by this workstation's AVG TLS-interception issue ([[avg-tls-interception]] memory) — a justification that doesn't hold up, since that issue has a one-line fix (`pip install truststore` + `truststore.inject_into_ssl()`, already confirmed working on this machine). No document shows the user's Python instruction being captured or the switch to Node being re-confirmed with them. An entire Phase 1 framework was built, and this session continued extending it (real GSC/DataForSEO connector implementations, `watchdog.mjs`, intake checklist) before the mismatch surfaced. Independently, the user asked Codex for a second opinion; Codex's read-only assessment also recommended Python for this specific codebase (small local orchestration/CLI tooling, not browser/Node-ecosystem-dependent) and to rewrite now rather than later, while the repo is still small. | Resolved | Full rewrite of `tools/` from Node.js (`.mjs`) to Python (`.py`), same day, all files: `lib/paths.py`, `lib/redact.py`, `lib/lock.py` (cross-platform PID-liveness check via `ctypes`/`OpenProcess` on Windows, since `os.kill(pid, 0)` actually terminates a process on Windows unlike Node's `process.kill(pid, 0)`), `spec_validate.py`, `snapshot.py`, `mock_metrics.py`, `gsc.py`, `dataforseo.py` (stdlib `urllib.request`, no new dependency), `run_loop.py`, `review_pending.py`, `apply.py`, `watchdog.py`, and the full `tools/tests/phase1_exit_criteria.py` integration suite — all 32 exit-criteria checks plus every module's `--verify` self-test pass identically to the Node version. All `.mjs` files, `package.json`, and `package-lock.json` removed; `node_modules/` was already gitignored. `_demo`'s run history was regenerated end-to-end via the Python engine (same two-consecutive-run proof: run 2 evaluates the applied proposal as a verified winner). All Node references in `CLAUDE.md`, the three skills, and `templates/` were updated to the Python equivalents. A standing rule was saved to the operator's persistent memory (`language-choice-approval` memory, referenced from `CLAUDE.md`): never pick a non-Python implementation language for this user's projects without explicit prior approval, regardless of a plausible-sounding technical reason. |
| R9 | Phase 2 Step 6 execution, 2026-07-16 to 2026-07-18 | Low | Phase 2 kickoff for the first real project (`art`) was explicitly user-authorized 2026-07-16. The live smoke test (`tools/smoke_test.py art`) and first real run (`tools/run_loop.py art seo`) both initially returned 0 rows/0 proposals on 2026-07-16 — Search Console had not yet finished processing data for the newly-verified `sc-domain:acceleratedrehabtherapy.com` property. Both were retried 2026-07-18 and succeeded with real data (177-row smoke test; 451-sample-size run producing 3 draft proposals). No connector, credential, or tooling defect — a normal data-availability lag on a freshly-verified GSC property. | Resolved | Retried after the property finished processing; no code change needed. `HANDOFF.md` and `PHASE2_READINESS_CHECKLIST.md` §5/§6 updated to reflect both the 0-row and real-data runs. `art` remains `propose-only` with all `allowed_actions` `manual_approval_only: true`; the 3 draft proposals from the 2026-07-18 run await Nate's `/review-pending` decision — no Tier-1 apply path exists for this project regardless of that decision. |
| R10 | Amends R6 — Plan (`PLAN.md` Phase 5), implemented and independently reviewed 2026-07-29/30 | Low | R6's original wording classified "any push, merge, or remote-tracking-branch update" as Tier 2/human-only. Phase 5 gave `apply.py` a real, tooling-performed remote-tracking-ref update: before creating a worktree it runs `git fetch origin +refs/heads/main:refs/remotes/origin/main` and refuses if the local `refs/heads/main` no longer matches `refs/remotes/origin/main` (baseline drift). Left as originally worded, R6 would read that fetch as already-forbidden Tier 2 — it isn't, and the distinction needs to be explicit rather than inferred. | Resolved | R6's Tier-2 boundary is **narrowed to what it always meant in practice: writes to the actual remote** — push, merge, deployment, and rollback. **Read-only** `git fetch` that only updates this checkout's own local remote-tracking ref cache (never opens a connection capable of writing) is Tier 0/tooling-permitted, and is now required before every Tier-1 apply attempt, refusing safely on drift, a missing ref, or a fetch failure. Local, unpushed `git worktree`/branch/commit creation remains Tier 1/tooling-permitted only for an approved, non-`manual_approval_only` proposal on a loop that has explicitly opted in (unchanged from R6/Phase 3). Push, merge, deployment, and rollback remain Tier 2/human-only, unconditionally — no code exists to perform any of them (`publish.py` is unbuilt, Phase 6). `art` stays `approval_mode: propose-only` with `manual_approval_only: true` on every action, so neither the fetch nor the worktree path fires for it today. Verified via `tools/tests/phase1_exit_criteria.py` (baseline-drift refusal, fully-qualified-ref usage, base-SHA persistence/backfill, ancestry-mismatch refusal, no-temp-dir-leak-on-refusal — 133/133) and `tools/lib/artwebsite_seo.py --verify`. |
| R11 | Plan (`PLAN.md` Phase 6), implemented and offline-tested 2026-07-30 | Medium | `tools/publish.py` was built: for an `implemented` proposal with a recorded human approval, it re-verifies commit ancestry, validates the diff from the commit's own git objects (exactly one changed file, blob contains the approved `new_value`), confirms the local branch hasn't moved, checks `.github/workflows/deploy.yml`'s trigger shape at the base commit, validates `origin`'s push URL, hard-refuses unless GitHub branch protection on `main` is independently confirmed live, then pushes to `seo/<id>`, opens a PR (`lib/github_compare.py`'s new `create_pull_request()`), and requests auto-merge (`enable_auto_merge()`, GraphQL). This is real push/PR/merge-request capability — the exact Tier-2 boundary R6/R10 draw the line at — landing in the codebase for the first time, so the register needs an honest entry even though nothing is activated. | Open — code shipped, live verification and activation both outstanding | **What is true today:** no project's `project.md`/`spec.md` references `publish.py`, no `<project>-github-token` credential alias has been stored, and no skill or automated path calls it — it is dead code from the perspective of every existing loop, callable only by a human running it directly with explicit `--owner/--repo` or a `LIVE_COMPARE_TARGETS` entry (currently only `art`, itself still `propose-only`/`manual_approval_only: true`, so `apply.py` never produces an `implemented` proposal for it to act on). **What is NOT yet true:** `PLAN.md` Phase 0 (separate automation identity, branch protection with required review + no-bypass + push restrictions, and the live fail-closed test matrix in Phase 0c) has not been performed against the real `nateginn/artwebsite` repo or a real GitHub credential — `publish.py`'s `verify_branch_protection()` hard-refuses on a missing/misconfigured/unqueryable protection response, so an unconfigured `main` fails closed by construction, but that logic itself has only been exercised against fake and local-fixture HTTP/git responses, never a real GitHub API response shape. Before any real use: (1) complete Phase 0 with the real credential per `PLAN.md`'s fail-closed matrix, (2) store a `<project>-github-token` alias for a separate (non-Nate) automation identity, (3) explicitly wire a project's config to call `publish.py` — none of which this session performed, per explicit user instruction. Verified offline via `tools/publish.py --verify` (32 checks, every refusal + success path with fake git/HTTP) and `tools/tests/phase1_exit_criteria.py` (5 new real-git-repo integration tests: end-to-end push+PR+auto-merge against a local bare origin, plus real `git diff-tree`/`.github/workflows/deploy.yml`/branch-protection/remote-URL refusals — 144/144 total). |
| R12 | Phase 6a implementation, offline-tested 2026-07-30 | Medium | Post-deployment health verification and rollback orchestration was added without activating production actions. | Open — orchestration implemented; production integration intentionally absent | `tools/lib/deploy_health.py` provides bounded DNS/TLS/HTTP/canonical/robots/representative-page checks and advisory commit evidence. `tools/deploy_verify.py` persists new and previous SHAs, waits non-blockingly for the stabilization window, records health/rollback outcomes, redacts every write, and escalates on rollback failure. Verified by 31 + 42 module checks and the full 153/153 suite. The default redeploy hook refuses rather than pushing or deploying; no project callback/scheduler is wired; no credentials or live deployment were used. `art` remains propose-only/manual-approval-only. |
| R13 | Phase 7 (`PLAN-PHASE7-CODEX-REVIEW.md`, six rounds of Codex adversarial plan review — `PLAN-REVIEW-LOG-PHASE7.md`), implemented and offline-tested 2026-08-03, built while the user was away from keyboard per his explicit prior authorization to use Codex review in place of questions that would otherwise go to him | Medium | A second, independent Codex review stage plus eligibility-gated **automatic local-commit implementation** (still the pre-existing Tier-1 boundary — never push/publish/deploy) was added for low-risk SEO proposals, landing real automatic-implementation capability in the codebase for the first time without any individual per-proposal human sign-off, for whichever loop explicitly opts in. Also: a new durable, event-sourced (for the new review subsystem) / best-effort (for the pre-existing state machine) event history, and an MMC briefing section reading it. | Open — code shipped and thoroughly offline-tested; unactivated for any real project | **What is true today:** `art`'s `spec.md` is unmodified (`propose-only`, `manual_approval_only: true` on every action, `auto_implementation_enabled` absent) — none of this fires for `art`. No project's spec has opted into any of the three required gates (`approval_mode: tier1-enabled` + `auto_implementation_enabled: true` + per-action `manual_approval_only: false`), so `apply.py`'s codex-review auto-implement path is unreachable for every existing loop. `.claude/skills/codex-seo-review/SKILL.md` is invoked on demand only (`/codex-seo-review`) — nothing registers it on any scheduler; true zero-touch operation would require a Claude-Code-capable scheduled agent (not Task Scheduler → bare `python.exe`, which cannot reach `codex exec` at all), a separate activation decision this session deliberately did not make. **What is NOT yet true / known, bounded limitations:** reviewer-record provenance is local accountability only, not cryptographic proof — any process with filesystem access to this repo could hand-craft two "approve" verdicts (mirrors the same already-accepted position on `publish.py`'s guards, not a new gap); the pre-existing legacy state machine's `event_log.py --reconcile` checks only the latest on-disk milestone per field, not full per-run history (a deliberately bounded, argued-back trade-off — see the plan's Key Decisions 7/9); `apply.py`'s second, pre-commit spec re-read narrows but does not eliminate the TOCTOU window on `spec.md`, which is not lock-protected anywhere in this codebase, before or after this work; `internal-link-addition` can be reviewed but never auto-implemented (no HTML-mutation engine exists or was built — a deliberate scope boundary, Key Decision 3). Verified offline via `tools/tests/phase1_exit_criteria.py` (191/191 — 153 pre-existing + 38 new, including a full end-to-end fixture with a real local git commit), every new/changed module's own `--verify` self-test (`event_log.py` 26/26, `review_protocol.py` 22/22, `draft_link.py` 17/17, `artwebsite_seo.py` 31/31, `spec_validate.py` +7, `draft_copy.py` +2), and MMC's `looping_events.py` (12/12) / `advance_looping_cursor.py` (9/9) self-tests plus a direct read-only smoke call of the real `looping.collect()`. No live SEO run, live GitHub call, live Telegram send, or touch to any existing pending proposal occurred during this work. |
