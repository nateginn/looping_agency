# Plan Review Log: Codex-reviewed low-risk SEO auto-implementation + MMC change-history briefing
Started 2026-08-03 (session time). MAX_ROUNDS=5. Nate is away from keyboard; per his explicit instruction this session uses Codex adversarial review in place of questions that would otherwise go to him.
The plan is not ready to implement. Material flaws:

- The “automatic” path is manual-only (`/codex-seo-review`) and no project is opted in; this does not satisfy the stated automation goal. Fix: explicitly scope it as on-demand capability or add a scheduled Claude-capable orchestrator with fail-closed controls.
- The plan depends on a “proven” `.claude/skills/codex-review/SKILL.md`, but that skill is absent from the repository. Fix: specify and test the complete invocation/output parser instead of relying on a nonexistent dependency.
- No transition moves `draft` to `review-pending`; `--record-round` assumes that state already exists. Fix: add an atomic `start-review` transition and event.
- Revision is impossible: `draft_copy.py` only accepts `status == "draft"`, while the plan changes the proposal to `review-revision-needed` before redrafting. Fix: permit one revision from that state with hash/version checks.
- `review-held` and `review-approved` with `eligible=False` are dead ends because `review_pending.py` cannot act on any new state. Fix: define human approve/reject/hold transitions for every new state.
- `apply.py` trusts mutable `proposal["auto_implementation"]["eligible"]`; a direct caller can forge that field and status without two Codex approvals. Fix: recompute authorization from the current spec, review hashes, evidence, and verdicts inside `apply.py`.
- The documented disable switch is ineffective after adjudication: `apply.py` does not re-check `auto_implementation_enabled` or the current action’s `manual_approval_only`. Fix: re-read and enforce both authoritative spec gates immediately before committing.
- `evaluate_auto_implementation_eligibility(proposal, spec)` cannot perform its stated checks because it needs `state.json`, repository path, and current file contents; it is also described as pure. Fix: pass an explicit read-only context or split I/O collection from pure policy evaluation.
- Verdict records are not bound to the exact evidence packet or current proposal hash, and duplicate/out-of-order rounds are not prohibited. Fix: require an exact packet hash, unique round number, reviewer identity, and current proposal version at recording time.
- Revision history stores hashes but not the actual before/after content or diff, so it cannot show what changed. Fix: persist a bounded immutable revision diff alongside each hash.
- Event writes are not transactionally coupled to proposal/state writes; crashes can produce events without transitions or transitions without events. `events.seq` can also diverge from `events.jsonl`. Fix: use an append-only outbox/journal with fsync and replay/reconciliation.
- Monotonic sequence IDs are not idempotent: retrying a crashed emission creates a second event with a new ID, contradicting “stable across rerun.” Fix: derive an idempotency key from proposal/version/transition and deduplicate under the lock.
- `event_log.py` has no secret inventory, yet it writes LLM-authored rationale, copy, and URLs; `redact_deep()` without the caller’s secret map cannot perform alias redaction. Fix: require a secret map/canary gate or restrict event fields to validated, bounded data.
- Existing `run_loop.py` does not cool down `review-pending`, `review-revision-needed`, or `approved-for-implementation` proposals, so repeated runs can create duplicate proposals for the same page. Fix: atomically claim target pages and include all active review/implementation states in cooldown.
- `draft_link.py` compares path-shaped proposal pages with absolute-URL `spec.priority_pages`, so valid destinations will be rejected unless normalized. Fix: canonicalize both to origin/path before comparison.
- The five noindex exclusions exist only in YAML comments; parsed YAML cannot cross-check comments. Fix: add a machine-readable exclusion list or perform an explicit indexability check.
- Internal-link review evidence does not persist source-page content, destination canonical/indexability evidence, or an opportunity proof; an anchor triple alone is insufficient for the promised checklist. Fix: store bounded source/destination evidence and hold proposals lacking it.
- MMC uses one cursor for a design that creates one event stream per loop; event IDs from different loop slugs are not globally ordered. Fix: store cursors keyed by `(project, loop)` or restrict the design to one explicitly configured stream.
- `advance_looping_cursor.py` rereads mutable `inputs.json` after delivery, so it can advance over events that were not in the briefing actually sent. Fix: bind the cursor update to the collected input hash/event-ID set embedded in the briefing run.
- `notify.py` exit code means Hermes/fallback accepted the send command, not necessarily that Telegram delivery completed. Fix: make cursor advancement depend on an end-to-end delivery acknowledgment.
- Malformed JSONL events are skipped, which can permanently lose accountability while the cursor continues advancing. Fix: mark the source degraded and refuse cursor advancement until corruption is repaired or explicitly acknowledged.

VERDICT: REVISE

### Claude's response

All 22 points accepted and incorporated into `PLAN-PHASE7-CODEX-REVIEW.md` round 1, except two argued back rather than accepted verbatim (both noted explicitly below — nothing silently dropped):

- **Accepted as-is**: wrong skill path (`.claude/skills/codex-review` is a global user skill, not repo-scoped — my new skill will be repo-scoped like `run-loop`/`review-pending` and will not assume the global skill's file is visible to a repo-sandboxed reader); missing `draft → review-pending` transition; `draft_copy.py`/new revision path for `review-revision-needed`; dead-end states in `review_pending.py` (fixed by widening its `TRANSITIONS` `from` lists so the *existing* human approve/reject can rescue any new state, landing in the same existing `approved`/`rejected` terminals — no new human-facing states to teach); `apply.py` must recompute eligibility from source, not trust a cached boolean (this single fix also closes the "disable switch ineffective" finding, since recomputing means it re-reads current `auto_implementation_enabled`/`manual_approval_only` every time); splitting I/O gathering from pure policy evaluation; binding verdict records to an exact packet hash + strictly increasing round numbers + refusing a stale/mismatched hash; storing actual before/after values (not just hashes) in the revision record; widening `run_loop.py` cooldown to every non-terminal status, not just `applied`; normalizing paths vs. absolute URLs in `draft_link.py`; making `noindex`/excluded-destination pages a real spec field, not a YAML comment; persisting source-page snippet + destination indexation evidence in the internal-link evidence packet; keying MMC's cursor by `(project, loop)`; stopping cursor advancement at the first malformed event rather than skip-and-continue past it.
- **Argued back, not accepted verbatim — event/proposal transactional coupling (finding 11) and idempotent event IDs (finding 12)**: a full fsync'd outbox/journal-and-replay system is real but disproportionate scope for this task on top of everything else already being built. Instead: proposal-state file write happens first (it is already, and remains, the sole source of truth — matches every existing tool's design), event append happens second, both inside the same already-held `run.lock` critical section. A crash between the two leaves a state transition with no matching event — a bounded, documented gap, not a silent one: `event_log.py` gains a `--reconcile <project> <loop>` maintenance command that scans for a proposal `status` with no corresponding event and backfills a `reconciled: true` event, and every emission site's caller already checks "is this transition actually new" (old status != new status, checked fresh under the lock, matching `apply.py`'s existing `_recover_attempt_if_needed` idiom) before calling append — so a retried, already-completed operation fails its own precondition check before it would ever re-emit, giving idempotency-by-construction rather than by a manufactured dedupe key. This is weaker than a real journal but proportionate; flagging it back to Codex explicitly in round 2 rather than silently downgrading the fix.
- **Accepted with a documented limitation, not a rebuild — `notify.py` delivery semantics (finding 20)**: `notify.py`'s success today already means "accepted by Hermes / sent via fallback API," not a Telegram read-receipt, for every other MMC data type, not just this one. Building end-to-end delivery acknowledgment is a pre-existing MMC-wide gap, not something this task's scope owns; the plan now says explicitly that "successful delivery" inherits `notify.py`'s existing definition, and that if that definition is ever tightened MMC-wide, this feature's cursor gets the fix for free.
- **New, not from Codex but needed to make the accepted fixes coherent**: `advance_looping_cursor.py` now refuses to advance if `inputs.json`'s mtime changed since `collect.py`'s own recorded write-time stamp (closes finding 19's "rereads mutable inputs.json" risk directly, since in the real pipeline nothing rewrites it between collect and notify — this makes that invariant enforced, not assumed).

Revising `PLAN-PHASE7-CODEX-REVIEW.md` to Round 1 now; resubmitting to the same thread.Round 1 fixes address many findings, but material problems remain.

- The structural event approach is insufficient for a durable audit: reconciliation cannot reconstruct intermediate transitions after later state changes, and MMC never automatically runs it. Fix: use an outbox/journal, or make reconciliation automatic and persist immutable transition records.
- `events.seq` can still duplicate IDs if the log append succeeds but the sidecar update crashes; precondition checks do not protect unrelated later events. Fix: reserve and durably persist the sequence before append, then reject any existing-ID collision.
- Status-based idempotency does not cover same-status events such as `review_round_completed`; a persisted round with a missing event cannot be reconstructed by current-status reconciliation. Fix: use per-transition/version idempotency keys or immutable round records.
- The “correctable hold” path cannot auto-approve: round 1 remains `hold`, round 2 may be `approve`, but `adjudicate()` requires both rounds to approve. Fix: require two fresh approvals after revision or explicitly supersede the first round and run another independent review.
- `apply.py` recomputation closes the cached-boolean and post-adjudication config-toggle holes in principle, but it revalidates round hashes against the current proposal; any legitimate revision makes round 1’s old packet hash fail. Fix: store immutable proposal/evidence versions and validate each round against its own version, requiring the latest round to cover the final content.
- Eligibility is gathered before the potentially long fetch/worktree/commit sequence, so a concurrent spec edit can change a gate after the check but before the commit. Fix: re-read/hash the spec and policy immediately before `git commit`, or lock the spec/config during the entire authorization window.
- Reviewer records remain forgeable by any process with filesystem access: an attacker can replace both verdicts with valid hashes and two approvals. Fix: either add authenticated reviewer artifacts/signatures or state clearly that the mechanism provides no provenance guarantee beyond local accountability.
- The broadened cooldown says every status except `rejected`, `verified`, and `breached` is active, but `review-rejected` is not excluded and can permanently block that page. Fix: define an explicit terminal-status set including `review-rejected`.
- Event redaction still lacks the connector secret map at `apply.py`, `draft_copy.py`, review-tool, and reconciliation call sites; calling `redact_deep()` without it does not provide alias redaction. Fix: pass the relevant secret inventory or restrict event values to validated non-secret fields.
- The mtime stamp does not bind `inputs.json` contents: same-resolution writes or a writer restoring the timestamp can pass the check while changing events. Fix: record and verify a SHA-256 plus size, ideally also bind that hash to the briefing artifact.
- Reusing `notify.py`’s success semantics is a defensible scope trade-off only if the requirement is explicitly “accepted by the send rail”; it is not equivalent to confirmed delivery, and cursor advancement irreversibly suppresses future reporting. Fix: either add a delivery acknowledgment or rename/document the guarantee as send-acceptance and obtain acceptance of that weaker contract.
- The on-demand skill and unactivated `art` choice are defensible safety decisions, but they mean this is a dormant capability rather than an operational automatic path. Fix: change the goal/acceptance criteria to “on-demand capability” or add a separately approved scheduler/activation phase.

VERDICT: REVISE
### Claude's response

Round 2 forced a real design change, not just patches — accepted:

- **Event log becomes the source of truth for the NEW review-protocol subsystem specifically** (findings 1-3): `--record-round` now writes the round record to `events.jsonl` first (the append IS the commit point for that round), then updates `proposal["review"]["rounds"]` as a projection rebuilt from the event log (`rebuild_review_rounds_from_events()`), used by `--reconcile`, by `apply.py`'s recomputation, and by any reader. `events.seq`'s sidecar is dropped entirely — sequence number is derived from the event log's own line count under the lock, so there is no second file to diverge (closes finding 2 by removing the thing that could diverge). Round completion is now idempotent on its natural key (round number), not a status comparison (closes finding 3). This is scoped to the review-protocol subsystem only — the pre-existing `draft/approved/implemented/applied/verified/breached` machine keeps its already-shipped, already-tested crash-recovery model untouched; events there stay best-effort observability (documented, bounded gap, unchanged from round 1) rather than rewriting proven code.
- **Adjudication redesigned to fix the correctable-hold logic bug** (finding 4, a real bug in round-1's design, not just a hardening request): a round is only eligible for adjudication if its recorded `evidence_packet_hash` matches the *current* proposal's content hash. Two independent rounds against the original content ("pass 1" — A, B); if they disagree or either holds with a correctable issue, one revision is allowed, then two fresh independent rounds against the revised content ("pass 2" — C, D; old A/B are superseded, excluded from adjudication by the hash filter, not deleted from the log). Adjudication always compares the two most recent rounds whose hash matches current content. Bounded at 4 Codex calls total. This also resolves finding 5 (stale-hash-after-revision) as a side effect, since old rounds are never compared against new content.
- **`apply.py` re-reads spec.md a second time immediately before the git commit call** (finding 6), re-validating every gate — not just the new eligibility checks but the pre-existing tier/`approval_mode`/`manual_approval_only` checks too, applied uniformly rather than only hardening the new code path. Flagging explicitly: the underlying TOCTOU window (spec.md isn't lock-protected) is a pre-existing characteristic of the whole codebase, not something this plan introduces; this closes it for old and new checks alike in one small addition since I'm already touching apply.py's gate logic this task.
- **Terminal-status set for cooldown is now an explicit named constant** including `review-rejected` (finding 8, a real bug).
- **`inputs.json` binding upgraded from mtime to sha256(content)** (finding 10).
- **"Successful delivery" is renamed/documented precisely as "accepted by the send rail," not "confirmed delivery"** throughout (findings 11/23), matching Codex's own suggested fix wording rather than my looser round-1 phrasing.
- **Redaction call sites**: accepted as a real open item, not yet resolved by argument — `tools/lib/redact.py`'s actual signature/secret-map requirement will be read and matched exactly (mirroring `snapshot.py`'s real usage) during implementation, not decided further in the plan text; a dedicated test (known alias/pattern doesn't leak through an event) is added to prove it rather than assert it.
- **Argued back, not rebuilt — reviewer-record provenance (finding 7)**: accepted as a true, permanent limitation, not a defect to fix. This mirrors `PLAN.md`'s own established, already-accepted position on `publish.py`'s guards ("not a security control against an agent that can run arbitrary commands") and its explicit rejection of cryptographic attestation as "over-engineered for a solo operator." The plan's documentation (item 10 of the original task) already commits to stating this plainly; round 2 sharpens the wording to be explicit that this is local accountability, not provenance proof, with no signature scheme — consistent with existing project precedent, not a new gap.
- **Argued back, precision fix only — dormant-vs-operational (finding 12)**: accepted that the Goal section overclaimed; reworded to state the delivered scope precisely (mechanism + on-demand invocation + full offline test coverage; scheduling/`art`-activation explicitly out of scope as separately-approved future steps) rather than imply a zero-touch operational path exists today. No mechanism change — the underlying decision from round 1 stands, argued again for the same reasons (unilaterally activating a recurring autonomous automation while the human sponsor is unreachable is a bigger, separate risk decision than building and proving the mechanism).

Revising to Round 2; resubmitting to the same thread.

## Round 2 — Codex
(appended above; header added for structure)
Round 2 improves the design substantially. The send-acceptance and local-provenance arguments are reasonable once explicitly documented, but material issues remain:

- Review-round events are commit points, but status transitions remain ordinary projection writes; a crash after `review_round_completed` and before setting `review-revision-needed` leaves the event log and state machine disagreeing. Fix: include resulting status in review events and replay status projections from the log.
- The legacy state-machine reconciliation still cannot detect a missing `implementation_created` event once a later `proposal_applied` event exists, so the accountability surface can omit the central local-commit action. Fix: reconcile each lifecycle milestone independently or event-source the entire auto-implementation path.
- Counting valid lines can reuse an ID after a truncated event line; appending afterward turns the corrupt tail into an interior line and can break review reconstruction. Fix: refuse appends when the tail is malformed, repair it under lock, and derive the next ID from the maximum valid sequence.
- The event-log “commit point” has no explicit flush/fsync requirement, so a successful append can still disappear on process or power failure. Fix: flush and `os.fsync()` the event file before treating the round as committed.
- The plan conflates `evidence_packet_hash` with the proposal content hash: `hash_proposal_for_review()` hashes only proposal fields, while the evidence packet also contains metrics and prior events. Fix: store distinct proposal-version and canonical-evidence hashes and validate the exact pair for each review round.
- Superseding rounds A/B is described, but no explicit immutable supersession event/replay rule is defined; rebuilding solely from `review_round_completed` events cannot know that A/B were superseded. Fix: emit a revision/supersession event containing the old content hash and round IDs.
- `apply.py` re-runs adjudication, but `evaluate_auto_implementation_eligibility()` still checks the mutable cached `review.final_adjudication`; the plan does not explicitly require the fresh adjudication result to equal `approve`. Fix: pass the fresh result into eligibility and ignore or cross-check the cached field.
- The redaction issue remains unresolved in the plan: `append_event(loop_dir, event_type, **fields)` has no specified secret-map input, while `redact_deep()` only performs alias redaction when given that map. Fix: define the secret-map plumbing explicitly or restrict events to validated non-secret fields before implementation.
- Item 28’s MMC test requirement still says to test an mtime mismatch, although the implementation now uses a SHA-256 content hash. Fix: change the test requirement to hash mismatch and add a same-mtime/different-content case.
- The revised scope is now honest about being on-demand and unactivated, so it is no longer an automation-goal mismatch; however, the plan should ensure the acceptance criteria and handoff consistently call this a dormant capability rather than an operational automatic system.

VERDICT: REVISE
VERDICT: REVISE

## Round 3 — Codex
(appended above)

### Claude's response

All 10 points accepted and incorporated into Round 3 of `PLAN-PHASE7-CODEX-REVIEW.md`:

- **Status is now itself a replayed projection for the review subsystem, not a separately-written field** (finding 1): every review-subsystem event (`review_started`, `review_round_completed`, `proposal_revised`, `review_held`/`approved`/`rejected`, `proposal_auto_approved`) carries the `resulting_proposal_status` it produces; `sync_proposal_projection()` replays the tail of a proposal's events and corrects `status`/`review` in memory (persisting the correction if it differs) at the top of every tool that touches a proposal in this subsystem — closing the "event committed, status write lost" crash window generally rather than at one call site.
- **Legacy-subsystem reconciliation redesigned around durable on-disk milestone evidence, not current `status`** (finding 2): `implemented_commit_sha`/`implemented_branch` present ⇒ expect `implementation_created`; `applied_at` present ⇒ expect `proposal_applied`; `evaluated_run_id` present ⇒ expect `proposal_verified`/`guardrail_breached` per `evaluation_outcome`. These fields are never cleared by later transitions (confirmed against `run_loop.py`'s actual code — `_evaluate_prior_experiments` doesn't clear `applied_at`, etc.), so each milestone is checked independently instead of only the latest.
- **Tail-corruption repair, not silent line-count reuse** (finding 3): before appending, `event_log.py` parses backward from EOF; if the tail is malformed, it truncates the file to the last fully-valid line boundary under the lock (the truncated data was never a durably committed event) before computing the next sequence number from the max valid `seq` parsed from surviving event IDs (not a line count) and appending.
- **Explicit flush/fsync** (finding 4): every append (including the repair-truncate) calls `f.flush(); os.fsync(f.fileno())` before the lock is released or success is returned.
- **Two distinct hashes, not one overloaded one** (finding 5): `proposal_content_hash` (existing `hash_proposal_for_review()` — action_type/target/implementation only, used for revision-supersession/adjudication content-matching) and a separate `evidence_packet_hash` (hash of the full packet a reviewer actually saw, including metrics/spec/prior events — used only to bind a verdict to the exact packet it reviewed, not for supersession logic).
- **Supersession is a derived replay computation, not a stored-and-trusted flag** (finding 6): given the design is bounded at exactly one revision (at most two distinct `proposal_content_hash` values ever exist across a proposal's rounds), "superseded" is computed at replay time as "this round's `proposal_content_hash` is not the most-recently-introduced one among all rounds for this proposal" — a pure function of already-recorded per-round hashes in event order, not a separately emitted/trusted event that could itself drift.
- **`evaluate_auto_implementation_eligibility()` now takes the freshly computed adjudication result as an explicit argument** (finding 7) and never reads `proposal["review"]["final_adjudication"]` for this decision — closing the residual gap where `apply.py`'s "re-run adjudicate()" and the eligibility check it feeds weren't actually wired together.
- **Redaction plumbing resolved by reading `tools/lib/redact.py`'s actual code, not deferred a third time** (finding 8): `redact_deep(value, secret_map=None)`'s alias-based redaction only ever fires on values in an explicitly passed `{alias: raw_value}` map; every existing caller (`snapshot.py`, `run_loop.py`) builds that map from live connector output, which none of the new event-emitting call sites (`apply.py`, `draft_copy.py`/`draft_link.py`, `review_pending.py`, `codex_review_proposal.py`) ever touch — they operate on proposal/derived data, not raw connector HTTP bodies. So `event_log.append_event()` calls `redact_deep(fields, secret_map={})` — this is honestly a **narrower** guarantee than `snapshot.py`'s (pattern-based `GENERIC_PATTERNS` redaction only, no alias-based redaction, since no call site has secrets to alias-map), stated as such rather than implied to be equivalent, with an explicit code comment that any future event-field source touching raw connector output must thread a real `secret_map` through, and a test proving the pattern-based path actually catches a fake key shape embedded in a test event field.
- **Item 28 test wording fixed**: mtime → sha256 content-hash mismatch, plus a same-hash-different-mtime non-issue case and a same-mtime(-touched)-different-hash refusal case.
- **Consistent "dormant capability" framing**: swept `HANDOFF.md`/skill-doc language items to use the same phrase as the Goal section, not just fix it in one place.

Revising to Round 3; resubmitting to the same thread.
Round 3 closes several prior gaps, and the send-acceptance/provenance trade-offs are now acceptably explicit. Material issues remain:

- `sync_proposal_projection()` is described as replaying the “event tail”; a bounded tail cannot reconstruct the authoritative status after older events roll out of that tail. Fix: replay the complete per-proposal event history or maintain a durable checkpoint with a verified sequence boundary.
- Human rescue transitions can still be overwritten unless `human_decision_recorded` is explicitly part of the review projection replay; otherwise `review-held → approved` is corrected back to `review-held` on the next sync. Fix: include human decisions and their resulting status in the replay schema.
- `review.final_adjudication` is still listed as projection data but the event payload requirements do not explicitly include it, so a replay can rebuild rounds/status while losing the adjudication result. Fix: include fresh adjudication and its result in adjudication events and replay it.
- The two-hash model is sound conceptually, but `adjudicate(rounds)` has no current proposal-content-hash argument even though it is specified to filter by one. Fix: make the current hash an explicit parameter and reject implicit derivation.
- Evidence packets include prior events, while round A’s event is appended before round B’s packet is validated; B can therefore receive A’s verdict or get a different packet hash, undermining reviewer independence. Fix: freeze one immutable packet per pass and exclude all in-flight review events from packet evidence.
- Tail repair can destroy committed-but-semantic-invalid or externally corrupted audit bytes, despite the log being called append-only. Fix: quarantine and report malformed suffixes, or only truncate a provably incomplete final write under an explicit recovery marker.
- `read_events()` is described as both read-only and repairing corruption; repair without the writer lock can race appenders and mutate data from a reader. Fix: keep readers pure and restrict truncation/fsync recovery to a lock-held append/recovery function.
- Legacy reconciliation still misses repeated evaluations because `evaluated_run_id` is overwritten rather than retained as per-run history; a missing earlier verification/not-evaluable event becomes undetectable. Fix: reconcile against immutable run artifacts or persist an evaluation-history list.
- Legacy reconciliation also lacks durable checks for `proposal_created`, `implementation_failed`, and `proposal_revised`, so the stated “each lifecycle milestone” guarantee remains incomplete. Fix: add never-cleared evidence fields or event checks for every emitted lifecycle event.
- Pattern-only event redaction remains a real secret-leak path: proposal text or source snippets can contain a credential that is not covered by `GENERIC_PATTERNS`, and the event can also be sent into the Codex evidence packet. Fix: pass a real secret inventory where available or enforce a strict non-secret field allowlist instead of relying on the documented limitation.
- `review_protocol.py` depends on `apply.IMPLEMENTABLE_ACTIONS` while `apply.py` now depends on `review_protocol`; a straightforward implementation creates a circular import. Fix: move the shared action set into a neutral constants module.
- The stated CLI forms still accept only `<id>`, although evidence gathering requires project/loop context and IDs are not globally resolvable. Fix: require validated `<project> <loop> <id>` arguments for every subcommand.
- The principal new pass-2 behavior is not exercised by the end-to-end test, which only covers two approving rounds on the original content. Fix: add an A/B correctable-hold or disagreement, revision, C/D approval, crash-replay, and supersession test.

VERDICT: REVISE
VERDICT: REVISE

## Round 4 — Codex
(appended above)

### Claude's response

11 of 13 points fixed as specified; 2 argued back explicitly (noted below). This is round 4 of 5 (MAX_ROUNDS=5, set at kickoff) — flagging that explicitly since Nate is unreachable to extend it and the skill's own rules say a cap that's hit without approval is a legitimate, documented outcome, not a failure, provided disagreements are surfaced rather than hidden.

- **"Tail" replay was imprecise wording, not a real bound (finding 1)**: per-proposal event counts are inherently small (at most ~15: created, review_started, ≤4 rounds, ≤1 revision, adjudication, implementation/apply/verify outcomes) — `sync_proposal_projection()` replays the **complete** filtered-by-`proposal_id` slice of `events.jsonl` every time, not a windowed tail. This also directly fixes finding 2 for free: replay processes every event type in chronological order including `human_decision_recorded` (already in the item-6 emission list), so a human rescue via `/review-pending` naturally becomes the latest event and is correctly reflected — no special-casing needed once replay is "everything for this proposal_id," not "the tail."
- **`final_adjudication` now an explicit event payload field (finding 3)**: the adjudication event's payload carries `final_adjudication` directly (not just `resulting_proposal_status`); replay reconstructs `proposal["review"]["final_adjudication"]` from the latest such event.
- **`adjudicate(rounds, current_content_hash)` — explicit parameter, no implicit derivation (finding 4)**.
- **One frozen evidence packet per pass, shared by both rounds in that pass (finding 5 — a real independence leak, not just a hygiene issue)**: building a fresh packet before each round meant round B's packet could include round A's own just-appended event as "prior context," leaking A's verdict into B and breaking independence. Now the packet is built once per pass (before round A), and the identical packet (same hash) is given to both rounds in that pass — round B never sees anything appended during its own pass.
- **Quarantine, not truncate-and-discard (finding 6 — correctly called out that deleting bytes from an "append-only" log contradicts the log's own premise)**: a malformed tail is copied to `events.jsonl.quarantine` (itself append-only, timestamped/reasoned) before the main log is truncated to its last valid boundary — nothing is silently lost, it's moved and flagged.
- **Readers stay pure; only the lock-holding writer repairs (finding 7)**: `read_events()` skips (never mutates) a malformed line; only an internal, lock-held `_append_with_repair()` (called from `append_event()`) performs the quarantine-and-truncate, and only immediately before its own append — closes the race with a concurrent unlocked reader (MMC's `looping_events.py` reads without Looping Agency's `run.lock`, so this distinction is load-bearing, not academic).
- **Reconcile extended to `proposal_created` (existence of `created_run_id`/`created_at`) and `implementation_failed` (`implement_error` present) — both cheap, existing, never-cleared fields (finding 9)**. `proposal_revised` is correctly a review-subsystem event (item 4's event-sourced path), not a legacy-reconcile concern — noted to avoid conflating the two subsystems' guarantees.
- **Real alias-based redaction restored via the existing credential-resolution pattern, not left as pattern-only (finding 10)**: `append_event()` gains an optional `secret_map` parameter; `codex_review_proposal.py` and `draft_link.py` (the two paths most likely to embed page/copy text) resolve the project's own configured `credential_aliases` transiently via `credentials.resolve_credential()` — exactly mirroring how `run_loop.py`'s connectors already build a secret_map for `snapshot.py`, no new mechanism — and pass the real map through. `apply.py`/`review_pending.py` do the same wherever `project` is in scope. Pattern-based redaction remains the floor for every call site regardless.
- **Circular import fixed by extraction, not restructuring either module (finding 11)**: `IMPLEMENTABLE_ACTIONS` moves to a new neutral `tools/lib/action_types.py`; both `apply.py` and `review_protocol.py` import from there.
- **Every new CLI subcommand takes `<project> <loop> <id>`, matching the existing tools' actual convention exactly (finding 12)** — `apply.py`'s real CLI is `apply.py <project> <loop> <proposal-id>`; my shorthand `<id>`-only descriptions were wrong, not a deliberate simplification.
- **End-to-end fixture broadened (finding 13)**: now explicitly exercises an A/B disagreement, a revision, C/D pass-2 approval, a simulated crash between round-event-append and projection-sync (proving replay recovers correctly), and a supersession check (old A/B rounds correctly excluded from adjudication after revision) — not just the original two-approving-rounds happy path.
- **Argued back, not extended — legacy-subsystem repeated-evaluation history (finding 8)**: accepted the narrower fix (extending reconcile to `proposal_created`/`implementation_failed`, above) but not the broader ask (persisting full per-run evaluation history for the pre-existing evaluator). This is a materially smaller gap than round 3's finding 2 (a real local-commit mutation potentially invisible) — an intermediate `not-evaluable` reading is not a mutation and isn't what the task's own event-type vocabulary centers on (proposal/implementation/verification/breach outcomes). Building real run-history for the legacy evaluator is the same class of scope expansion Key Decision 7 already declined for the whole legacy subsystem; extending it now for one narrow sub-case would be inconsistent with that already-argued, already-partially-accepted position. Flagging back to Codex explicitly rather than silently narrowing scope.
- **Argued back, not built — allowlist-instead-of-documented-limitation for redaction (finding 10's stronger form)**: the real alias-based redaction fix above closes the practical gap for known credentials; a strict non-secret field allowlist on top of that (rejecting/stripping any free-text field entirely rather than redacting known patterns within it) would mean `source_snippet`/rationale/copy text could never be shown at all, defeating their purpose in the evidence packet and the briefing. Pattern-based + real alias-based redaction, applied to genuinely public marketing-page text (not authenticated/internal content — this loop never reads anything but the operator's own public website), is judged proportionate; a full allowlist redesign is not adopted.

Revising to Round 4; resubmitting to the same thread. This will be treated as the final round if Codex still finds material issues — MAX_ROUNDS=5 is reached next, and per the skill's own rules a capped deadlock is surfaced honestly (not hidden) and handed to best-available judgment, since the human tie-breaker is explicitly unavailable this session.
Round 4 is substantially stronger, but I still find material issues.

1. The frozen packet is not actually persisted: `--record-round` rebuilds it after round A, while the packet includes mutable spec, snapshot, source, and event context. Round B can fail hash validation or see different evidence.  
   Fix: persist one canonical packet artifact per pass and have both rounds validate against that exact artifact.

2. MMC can skip events created after collection: the cursor check hashes `inputs.json`, but then rereads the live event log and advances to its current maximum, including events absent from the sent briefing.  
   Fix: advance only to the event-ID bound stored inside the hash-bound collected input.

3. `sync_proposal_projection()` and `rebuild_review_rounds_from_events()` skip malformed lines, despite being authorization-adjacent projections. A later event can make incomplete history appear authoritative.  
   Fix: return a corruption state and fail closed for projection, adjudication, apply, and cursor advancement; only advisory readers may return partial data.

4. Quarantine durability is underspecified: the main log may be truncated before the quarantine copy is flushed and fsynced; syntactically valid but schema-invalid events are also not clearly handled.  
   Fix: validate the event schema, fsync quarantine before truncation, and refuse the append if quarantine durability fails.

5. Fresh eligibility closes the cached-boolean and disable-switch holes, but `tier == 1` is still described as a proposal-field check. A forged proposal can claim tier 1 while the current spec action is tier 2; the schema permits tier-2 actions with rollback and `manual_approval_only: false`.  
   Fix: authorize from the current matching `allowed_actions` entry and require that entry’s tier to be 1.

6. The second spec read is not implementable at the claimed boundary without changing `finalize_worktree()`: the current function performs rewrite, `git add`, and `git commit` internally ([artwebsite_seo.py](/D:/Dev/Looping%20_agency/tools/lib/artwebsite_seo.py:363)).  
   Fix: split the commit step or add a pre-commit callback that revalidates immediately before `git commit`.

7. The “never-cleared” reconciliation premise is false for `implementation_failed`: existing `apply.py` removes `implement_error` after a later successful retry ([apply.py](/D:/Dev/Looping%20_agency/tools/apply.py:177)), so a failed attempt can disappear from reconciliation.  
   Fix: persist an append-only implementation-attempt/failure history or never clear the failure evidence.

8. Resolving every configured credential alias during control-plane event writes can fail on an unavailable or unrelated credential and leave operations partially mutated or unusable.  
   Fix: preflight a complete secret map before mutation and fail before state changes, or restrict the affected event fields to a guaranteed non-secret schema.

9. Hash-based supersession cannot distinguish a revision that returns to the original content hash (`H1 → H2 → H1`): old-pass rounds can be classified as current or mixed into a pair.  
   Fix: assign an immutable pass/version ID and adjudicate only rounds from the current pass.

10. The evidence hash binds the old spec into the packet, but apply does not require the current spec to match the reviewed spec; a policy/goal change that leaves the basic gates true can still apply an old approval.  
    Fix: store a canonical spec/policy hash in the packet and refuse apply when it differs.

11. Both Codex sessions receive untrusted source/copy text, but the plan does not define a prompt-injection boundary. The same malicious instruction can influence both “independent” reviewers.  
    Fix: serialize evidence as explicitly untrusted data, isolate it from instructions, and require reviewers to ignore embedded commands.

The two argued-back trade-offs are reasonable within their stated limits: legacy state-first events are an acceptable best-effort observability compromise, and `send-acceptance` is a defensible at-least-once contract if duplicates are tolerated. They do not resolve the concrete correctness gaps above.

VERDICT: REVISE
VERDICT: REVISE

## Round 5 — Codex (final scheduled round, MAX_ROUNDS=5)
(appended above)

### Claude's response

Codex confirmed both round-3/4 argued-back trade-offs as "reasonable within their stated limits" and opened with "substantially stronger" — but found 11 more issues, several genuinely serious (tier forgery, prompt-injection boundary, a hash-collision edge case, an implementability gap). **Decision: extending one round past the stated MAX_ROUNDS=5**, a deliberate judgment call, not a silent rule violation — logged here explicitly. Reasoning: these findings are legitimate, cheaply fixable, and close real holes in a mechanism whose entire purpose is catching exactly this class of risk; stopping now and merely *documenting* a known tier-forgery or prompt-injection gap in a review gate would undercut the feature's own point in a way the earlier argued-back items (which were genuine proportionality calls on secondary robustness, not primary-purpose holes) did not. If round 6 still finds material issues, that becomes the real, final, surfaced disagreement — no further extension.

All 11 fixed:

1. **Packet is now a persisted artifact, not a re-derivable claim**: `--evidence-packet` writes it once per pass to `pending/.review-artifacts/<id>-pass<N>.json`; both rounds in that pass, and `--record-round`'s hash check, read from that exact file — never rebuilt mid-pass.
2. **MMC cursor tightened to unambiguous wording**: `advance_looping_cursor.py` reads the event-ID bound *exclusively* from the already-hash-verified `inputs.json` snapshot's embedded `change_events.events` list — explicitly never re-queries `events.jsonl` directly (this was already the intent; round 4's wording left it ambiguous enough to implement wrong).
3. **Authorization-adjacent replay fails closed on corruption; advisory reads stay best-effort**: `sync_proposal_projection`/`rebuild_review_rounds_from_events`, when called from `--adjudicate` or `apply.py`'s auto-implement path, refuse outright if *any* malformed/schema-invalid line exists anywhere in that proposal's event history — a partial, silently-incomplete history must never look authoritative for an authorization decision. General/MMC reads remain best-effort partial (a different risk profile — observability, not authorization).
4. **Quarantine durability sequenced and schema validation added**: write quarantine copy → flush+fsync quarantine → *only then* truncate+fsync the main log; if the quarantine write/fsync fails, abort the whole append untouched rather than risk any loss. Every line, not just JSON-parseable ones, is schema-validated (required fields/types); a syntactically-valid-but-schema-invalid line is treated identically to a malformed one.
5. **Tier/manual_approval_only authorized from the current spec's matching `allowed_actions` entry, never the proposal's own cached fields** — closes a real forgery vector (a proposal claiming `tier: 1` while the spec's current entry for that `action_type` is actually tier 2, or vice versa if the spec changed).
6. **`artwebsite_seo.py::finalize_worktree()` gains an explicit `pre_commit_check` callback parameter**, invoked after `apply_rewrite()` but before `git commit` — this is a genuine code change to an existing function's signature, now named explicitly in the plan rather than assumed to already support a hook point that doesn't exist.
7. **"Never-cleared" corrected to be factually accurate**: `implement_error` *is* cleared by `apply.py` on a later successful retry (confirmed at the cited line) — the `implementation_failed` reconcile check has the same known blind spot as Key Decision 9's already-accepted legacy-history gap, not a new premise; wording fixed to stop claiming otherwise.
8. **Credential resolution for redaction is preflight and best-effort, never blocking**: every configured alias is *attempted* before any state mutation; a single alias failing to resolve is caught and simply excluded from that operation's secret_map (pattern-based redaction remains the unconditional floor) — never aborts or partially-mutates.
9. **Pass number, not content hash, selects the adjudicated pair** — closes a real edge case (a revision that returns to the exact original content, `H1→H2→H1`, would otherwise let hash-matching mix rounds from different passes). Each round event now carries an explicit `pass_number` (1 or 2, capped); `adjudicate(rounds)` takes the two rounds with the maximum `pass_number`, refusing if that pass doesn't yet have exactly two. Content hash still gates packet freshness at record-time, but is no longer the adjudication selector.
10. **Spec/policy drift between review and apply is now checked**: the evidence packet records a `spec_content_hash` (goals, exclusions, `allowed_actions`, `auto_implementation_enabled`, `noindex_destination_pages`); `apply.py`'s fresh eligibility check refuses if the current spec's hash differs from what was reviewed — an old approval cannot ride through under a since-changed policy.
11. **Explicit prompt-injection boundary, previously entirely unaddressed**: the review prompt wraps the evidence packet in an explicit untrusted-data delimiter with an instruction that any embedded text resembling a directive (e.g. "ignore previous instructions," a request to output a verdict directly) must be flagged as a suspected injection attempt in `objections` and treated as an automatic hold regardless of other content — added as an explicit checklist item alongside the task's existing section-5 review-quality list.

Revising to Round 5 (one past the stated cap, per the reasoning above); resubmitting to the same thread as the true final round.
Round 5 fixes most prior defects, but material inconsistencies and one replay gap remain.

1. The plan still contains stale algorithms: `--record-round` says it rebuilds the packet, and `--adjudicate` still calls `adjudicate(rounds, current_content_hash)` despite the new persisted-artifact/pass-number design.  
   Fix: globally replace those descriptions and tests with “load immutable pass artifact” and `adjudicate(rounds)`.

2. `proposal_revised` is an event-sourced commit point, but its payload is not specified to contain the revised implementation. A crash after the event append can leave status/projection indicating pass 2 while the proposal file still contains the old implementation.  
   Fix: include and replay the complete before/after implementation in `proposal_revised`.

3. `spec_content_hash` omits `approval_mode`, although it is an authorization gate. Changing `propose-only` to `tier1-enabled` could activate an approval made under the old policy.  
   Fix: hash every authorization input, including `approval_mode`, and test both directions of that change.

4. Tail repair still does not detect a malformed line in the middle of an otherwise valid log; proposal-scoped replay may be unable to associate that line with a proposal and therefore fail to enter the new fail-closed path.  
   Fix: validate the entire log before authorization/append, or maintain a durable global-corruption marker that blocks all authorization reads.

5. MMC’s reader is specified to stop on an “unparseable” line, but not explicitly on schema-invalid JSON lines. It could advance past structurally invalid events.  
   Fix: share the event schema validator with MMC and stop/cap advancement on any schema violation.

6. The packet artifact is called durable and persisted once, but overwrite refusal, atomic publication, and artifact fsync are not specified. A partial or replaced artifact breaks the “both reviewers saw identical bytes” guarantee.  
   Fix: write atomically, fsync, refuse overwrite for an existing pass artifact, and validate all three hashes from that file.

7. The prompt-injection delimiter is itself supplied alongside arbitrary page text; page content containing the closing delimiter can escape the intended boundary.  
   Fix: pass evidence through a structured/length-delimited or safely escaped data channel rather than literal unescaped delimiters.

The legacy best-effort event trade-off and `send-acceptance` cursor contract remain reasonable when explicitly treated as bounded observability/at-least-once guarantees, not security or delivery proofs.

VERDICT: REVISE
VERDICT: REVISE

## Round 6 — Codex (one round past MAX_ROUNDS=5, deliberate extension)
(appended above)

### Claude's response — concluding the adversarial-review phase

Codex's opening line ("fixes most prior defects") and closing line (both argued-back trade-offs "remain reasonable when explicitly treated as bounded") are the strongest convergence signal across six rounds — the remaining 7 findings are narrow consistency/completeness fixes and one deeper hardening item (#4), not fresh design flaws. All 7 accepted and folded into `PLAN-PHASE7-CODEX-REVIEW.md` Round 6 (final):

1. Stale cross-references (`--record-round` still described as "rebuilds the packet"; `adjudicate(rounds, current_content_hash)` still appearing after the pass-number redesign) — editing artifacts from earlier rounds that I missed sweeping. Fixed globally to match the final design (`adjudicate(rounds)`, "loads the immutable pass artifact").
2. `proposal_revised` events now carry the full `implementation_before`/`implementation_after` objects, and `sync_proposal_projection` restores `proposal["implementation"]` from the latest such event, not just `status`/`review` — closing the crash window where status could say "pass 2" while the on-disk proposal still held pass-1 copy.
3. `spec_content_hash` now includes `approval_mode` alongside goals/exclusions/`allowed_actions`/`auto_implementation_enabled`/`noindex_destination_pages` — a `propose-only → tier1-enabled` flip now correctly invalidates a prior approval.
4. **Global corruption marker, not just tail/proposal-scoped detection**: any read (not only the appending writer) that encounters a schema-invalid or malformed line *anywhere* in `events.jsonl` — not just this proposal's slice — writes/checks a lightweight `events.jsonl.corrupt_marker`; `--adjudicate`/`apply.py`'s auto-implement path check for this marker's existence first and refuse unconditionally if present, regardless of whether the bad line is even associated with the proposal in hand. Log-size stays small at this scale, so a full-log check is cheap.
5. MMC's reader shares the same required-field/type schema check as `event_log.py`'s (necessarily duplicated code across the two separate repos/venvs, documented as such, not a cross-repo import) and stops on a schema-invalid line exactly as it already does for a JSON-parse-invalid one.
6. Pass artifacts are written via the existing `atomic_write_json` utility (already used elsewhere in this codebase) plus explicit fsync, and `--evidence-packet` refuses to overwrite an existing `<id>-pass<N>` artifact — a re-run returns the existing file rather than replacing it, preserving "both rounds saw identical bytes" even across a retried skill invocation.
7. **Nonce-based delimiters, not a bare literal string**: the untrusted-evidence wrapper uses a fresh random token per invocation (e.g. `<untrusted-evidence-{nonce}>...</untrusted-evidence-{nonce}>`, nonce never derived from the evidence content), so evidence text cannot forge a matching close tag it doesn't know in advance; the prompt additionally instructs that any apparent occurrence of delimiter-like patterns *within* the evidence is itself a red flag to report.

**Concluding the adversarial-review phase here, at round 6.** I told Codex this would be the true final round regardless of outcome, and the substance of this round's critique (narrow completeness fixes, not new structural flaws, plus explicit re-confirmation of the two previously argued-back trade-offs) supports that this is a legitimate stopping point rather than premature closure — continuing to spin further rounds on ever-narrower points has real diminishing returns against the actual remaining work (implementation, tests, docs) this session still owes. The 7 fixes above are carried into the actual implementation directly; they are not re-verified by a further live Codex round. This is recorded honestly, not as "APPROVED," in case a future session wants to resume the loop.

---
**Plan concluded at Round 6.** Proceeding to implementation, tests, and documentation per the approach in PLAN-PHASE7-CODEX-REVIEW.md.
