# Loop Agency — operating instructions

**Read `CURRENT-WORK.md` first.** It is the live working record — what is being worked on now, the standing decisions that govern it, and the backlog. The `PLAN-*.md` files are the historical design record and are superseded by it for anything current. Working agreement: **one issue at a time**, fixed and confirmed before the next is started.

This workspace runs durable business loops (build → verify against a real KPI → learn → repeat) for onboarded client projects. Full design and rationale: `AgentColabPlan.md`. Review history: `PLAN-REVIEW-LOG.md`. Open findings and risk acceptances: `RISK-REGISTER.md`. Read all three before making structural changes here. This is a solo repo: commit directly to master, never create feature branches unless I explicitly ask

**Selected operating model for getting an approved change live: Path B — direct push to the deployment branch, then post-deploy health verification, rollback on failure, and notification through MMC's daily briefing.** Read `PLAN.md`'s "SELECTED OPERATING MODEL" section before touching anything on that path; most of the rest of `PLAN.md` describes Path A (side branch → PR → GitHub auto-merge) and is marked `[HISTORICAL — PATH A]`. `tools/publish.py` and the branch-protection/PR/auto-merge block of `tools/lib/github_compare.py` implement Path A and are **dormant by decision** — complete, tested, unreachable, and structurally unable to perform Path B (`publish.py` refuses a `main`/`master` destination by construction). Do not delete them; do not build on them. **Path B itself is not built: nothing in this workspace pushes anything, and R6's Tier-2 boundary has not been amended to permit it.** The autonomous half that *is* live is everything up to the local commit (`/codex-seo-review` → `apply.py`, see Phase 7 below).

## Phase status

**A second, separate phase plan exists for a new loop, `art/gbp` (Google Business Profile Posts) — its own "Phase 1" through "Phase 7" numbering, unrelated to the SEO-loop phases below despite sharing the same numbers.** Phases 1–4 shipped 2026-08-31 (Maps/local-pack connectors, the loop scaffold, a content-gap proposal selector, and a drafting tool gated by a cross-repo constraint check against `gbp-profiles.md`/MMC's `art.yaml`) — built and hardened via 14 rounds of Codex review while the operator was away, committed locally, not pushed. The loop is scaffolded but **inert**: not in `project.md`'s `loops_enabled`, not scheduled, and no publish path exists at all — nothing in this workspace can create or edit a live GBP Post. Phases 5 (partially — an event-log field extension only) through 7 are either unbuilt or blocked on the operator's own Google Cloud/API setup (Phase 0 of that plan). Full record: `CURRENT-WORK.md`'s "GBP Posts loop — Phases 1–4 built" section, `RISK-REGISTER.md` R19, `projects/art/loops/gbp/spec.md`, and this file's own "Tools" section below.

**Phase 7 (Codex-reviewed low-risk SEO auto-implementation + MMC change-history briefing) shipped 2026-08-03 — activated for `art/seo` the same day, then DEACTIVATED 2026-08-16 (`art` is back on `approval_mode: propose-only` + `auto_implementation_enabled: false`; see `CURRENT-WORK.md` "Automation is off, for real"). The capability is built and tested but is currently live for no project.** Full design record: `PLAN-PHASE7-CODEX-REVIEW.md` (six rounds of Codex adversarial plan review, `PLAN-REVIEW-LOG-PHASE7.md`). A second, independent review stage — Codex, invoked through the repo-scoped `.claude/skills/codex-seo-review/SKILL.md`, never Python alone (only a Claude Code Bash context can shell out to `codex exec`) — sits between drafting and implementation for a proposal. Two independent Codex rounds per pass (never `resume`, so neither round can defer to the other), bounded at one revision / two passes / four total Codex calls; agreement is required, any disagreement holds. The proposal state machine gains `review-pending`, `review-revision-needed`, `review-approved`, `review-rejected`, `review-held`, `approved-for-implementation` alongside the unchanged existing states; a human can always rescue any of these into the existing `approved`/`rejected` terminals via the unchanged `/review-pending` path. A new durable, append-only, redacted event history (`projects/<slug>/loops/<loop>/events.jsonl`, `tools/lib/event_log.py`) is the source of truth for this new review subsystem specifically — event append IS the commit point, proposal `status`/`review`/`implementation` are projections replayed from it (`sync_proposal_projection`), fail-closed on any log corruption for authorization-adjacent reads. The pre-existing `draft/approved/implemented/applied/verified/breached` machine is unchanged and keeps its own proven crash-recovery model; events there are best-effort observability (`event_log.py --reconcile`). Automatic implementation (still only ever a **local, unpushed git commit** — the pre-existing Tier-1 boundary; push/publish/deploy remain untouched and Tier 2/human-only, unconditionally) requires **three independent, explicit spec gates**, all opt-in: `approval_mode: tier1-enabled` (pre-existing) + new `auto_implementation_enabled: true` + the specific action's `manual_approval_only` absent/`false`. `apply.py` recomputes eligibility fresh from the *current* spec at every entry (never trusts a cached `proposal["auto_implementation"]["eligible"]` field, which is output-only) and again immediately before the actual `git commit` via a new `finalize_worktree(pre_commit_check=...)` hook — flipping any gate off after adjudication genuinely disables the path. `internal-link-addition` gets full evidence/drafting/review support but can never reach automatic implementation (no code mutates a live Django template to insert a link — `apply.py`'s `IMPLEMENTABLE_ACTIONS` still excludes it) — a deliberate scope boundary, not an oversight. **`art`'s spec was activated 2026-08-03 — `approval_mode: tier1-enabled` + `auto_implementation_enabled: true`, with `manual_approval_only: false` on `title-tag-rewrite`/`meta-description-rewrite` only (`internal-link-addition` stays manual-only, and can never auto-implement regardless — no HTML-mutation engine exists). This capability was built, tested (191/191 Looping-Agency-side checks, all offline, no live SEO run/GitHub call/Telegram send at build time), and documented before activation; see `RISK-REGISTER.md` R14 for the activation record and the scheduler-suitability finding (no durable, local, Claude-Code-capable scheduler exists in this environment that can reach the local `codex login` session, so `/codex-seo-review` stays invoked on demand, not scheduled).** **That activation was reversed on 2026-08-16** — `art` is `propose-only` + `auto_implementation_enabled: false` again, because standing decision 6 ("automation stays off until 'did it work?' is trustworthy") and `SEO_PROGRAM.md` Part 3A had both said so while the spec still said otherwise. Nothing was ever auto-implemented under it: zero proposals reached `applied`, and `pending/` was empty at the reversal, so no proposal carries a stale cached `manual_approval_only: false`. `approval_mode` is the switch that closes both doors (`apply.py:216-222`, read fresh at apply time, refuses every Tier-1 apply on any path); `auto_implementation_enabled` alone gates only the Codex path (`apply.py:233`). Separately, MMC's daily briefing gains a "Looping Agency changes since previous briefing" section (`D:\Dev\MMC\collector\sources\looping_events.py`, `collector\advance_looping_cursor.py`, `PROMPT.md`), backed by that same event log; MMC's cursor advances only after `notify.py` reports send-acceptance (never before, never a stronger "confirmed delivery" claim), bound to a sha256 content stamp of the exact `inputs.json` that was sent, so a failed briefing never loses events and a successful one never repeats them. **This mechanism is a quality/accountability control, not a security boundary** — any process with filesystem access to this repo could hand-craft two "approve" verdicts; no signature/attestation scheme exists, matching this project's own established position on `publish.py`'s guards (see "Where the security boundary actually is" in `PLAN.md`).

**Phase 2 (first real project, SEO loop) is the current phase — kickoff explicitly user-authorized 2026-07-16. `CURRENT-WORK.md` is the live record; `HANDOFF.md` is the older execution plan and carries its own staleness warning. `art` moved off `propose-only` on 2026-08-03 (`RISK-REGISTER.md` R14) and moved BACK to it on 2026-08-16 (R18) — it is `propose-only` today. The rank collector's three-defect rewrite the same day is R17.** The credential resolver (`tools/lib/credentials.py`), real gsc/dataforseo wiring in `run_loop.py`, and the connectors-only smoke test (`tools/smoke_test.py`) are built and tested; credentials are stored by the human only, in their own terminal (never ask for a raw secret in chat). Content-social and ads loops exist only as draft specs (`templates/loops/{content-social,ads}/spec.md`) — no connectors are wired for them, and `run_loop.py` will refuse to run them. The operator's website repo (`D:\Dev\artwebsite`) auto-deploys on push with no staging gate — **push, merge, deployment, and rollback are Tier 2, human-only, always (R6); no tooling ever performs them.** Read-only `git fetch` (to verify the local `main` baseline before an apply) and local, unpushed `git worktree`/branch/commit creation are tooling-permitted, but only for an approved, non-`manual_approval_only` proposal on a loop whose `approval_mode` has explicitly opted in — see `PLAN.md` Phase 3/5 and `RISK-REGISTER.md` R6/R10. `art` moved to `approval_mode: tier1-enabled` on 2026-08-03 and **back to `propose-only` on 2026-08-16**, so no project currently opts in and `apply.py` refuses every Tier-1 apply for `art` today; the `manual_approval_only` values (false on `title-tag-rewrite`/`meta-description-rewrite`, true on `internal-link-addition`) are unchanged and only matter if `approval_mode` is restored. This path is exercised only through the Codex-reviewed Phase 7 mechanism (`/codex-seo-review`) — a human approving a proposal directly via `/review-pending` also still reaches this same local-commit `apply.py` path, unchanged from Phase 3. **`tools/publish.py` (PLAN.md Phase 6) now exists** — the only component in this workspace permitted to touch a remote GitHub repo: pushes a validated `implemented` proposal's commit to `seo/<id>`, opens a PR against `main`, and requests GitHub auto-merge. It hard-refuses unless branch protection on `main` is independently verified live (PR + ≥1 review + no force-push/deletion + no admin/reviewer bypass), so it is only as safe as Phase 0's server-side controls, which have **not** been set up or proven with a real credential yet (`PLAN.md` Phase 0, `RISK-REGISTER.md` R11). It has never been run against real `artwebsite`/GitHub — every path is covered offline only (fake git/HTTP layers plus real-git-repo integration tests against a local bare origin). No project has an opted-in GitHub token alias or is wired to call it; nothing in this workspace invokes `publish.py` automatically.

## Implementation language

**Python only.** This workspace's tooling was rewritten from a Node.js prototype to Python on 2026-07-16 — see `RISK-REGISTER.md` R8. Never introduce a non-Python implementation language for this project's tooling without the user's explicit prior approval, even if there's a plausible-sounding technical reason to prefer something else (see `AgentColabPlan.md` and the memory note this incident produced).

## Environment setup

A project-local virtual environment lives at `.venv/` (gitignored — never commit it). Dependencies are pinned in `requirements.txt`: PyYAML, keyring, google-auth + requests (GSC service-account token minting), and truststore (Windows cert store for live HTTPS — see RISK-REGISTER.md R8).

```
python -m venv .venv                              # first time only
./.venv/Scripts/python.exe -m pip install -r requirements.txt   # first time / after requirements.txt changes
./.venv/Scripts/python.exe tools/run_loop.py <project> <loop>   # run any tool through the venv
```

Run tools through `.venv/Scripts/python.exe`, not a bare `python`, so the pinned dependency versions are what actually execute.

## The run contract (every run, every loop)

1. Acquire the per-loop run lock (`tools/lib/lock.py`). A live lock refuses the run and logs the refusal to `lock-refusals.log`. A stale lock (dead PID or age > `max_run_duration_minutes`) is auto-recovered: archived under `runs/<old-run-id>/stale-lock.json`, never manually deleted.
2. Validate `spec.md`'s YAML frontmatter against the schema (`tools/spec_validate.py`). An invalid spec refuses to run before anything else happens.
3. Load `memory.md` and the pending proposals (the experiment register).
4. Pull metrics via the loop's connector; write an immutable, redacted, timestamped snapshot under `runs/<run-id>/snapshot.json` (`tools/snapshot.py` — read-only file mode). **Connector failure is classified, not uniform** (`connector_registry.py`'s `critical` flag, added 2026-08-10): a *critical* connector (`gsc`, `dataforseo`, `mock` — the sources that feed `search_analytics`) still aborts the whole run as `partial-failure`, writing no snapshot, because evaluating proposals against a carried-forward stale section is the false-win failure mode Phase 1 exists to prevent. An *enrichment* connector (`pagespeed`, `gsc-indexation`, `dataforseo-local-rank`, `dataforseo-backlinks`) instead degrades the run: its failure is recorded as a failed `tool_call`, named in `degraded_connectors` and in report.md's Needs Attention, its snapshot section is carried forward from the previous run, run status becomes `degraded`, and the evaluation/proposal steps continue. `pagespeed.py` additionally retries transient 5xx/transport failures (3 attempts, 2s/5s backoff; never retries a 4xx).
5. Evaluate prior `applied` proposals whose observation window has elapsed and sample size is sufficient. A guardrail breach halts the loop (`state.json: paused-breach`) and blocks all new proposals until a human resolves it via `/review-pending --resolve-breach`.
6. Cooldown: no new proposal targets a page/asset that already has an `applied` proposal still inside its observation window.
7. Pick up to 3 new proposals from `allowed_actions` (skipped entirely if `paused-breach`); write them to `pending/` as `draft`.
8. Write `runs/<run-id>/run.json` (structured, redacted) + `report.md` (human-readable); append one summary line to `memory.md` (a derived view — `runs/` is the system of record). Release the lock.

**Approval state machine:** `draft → reviewed → approved → applied → verified` (or `rejected` / breach → revert proposal). Only `/review-pending` + explicit human approval can move a proposal to `approved`; only `tools/apply.py`, and only on an `approved` Tier ≤ 1 proposal, performs `applied`. Tier 2 proposals are never applied by tooling — human-only, always, including any `git push` to a repo that auto-deploys with no staging gate (see RISK-REGISTER.md R6). **Phase 7 (dormant by default, see above) adds a parallel path**: `draft → review-pending → [review-revision-needed →]? review-approved|review-rejected|review-held|approved-for-implementation`, converging on `apply.py` only from `approved-for-implementation` (never `approved` — the human and Codex-review paths write distinct proposal fields, `decision` vs `review`/`auto_implementation`, and are never conflated). A human can rescue any of these states into `approved`/`rejected` via the unchanged `/review-pending`.

## Tools (`tools/`)

- `spec_validate.py` — schema validator, `--verify` self-test.
- `lib/lock.py` — run lock + stale-lock recovery, `--verify` self-test.
- `lib/redact.py` — alias-based + pattern-based secret redaction, `--verify` self-test.
- `lib/paths.py` — canonical (symlink-resolved) path containment checks; every project/loop path is checked with `assert_within` before use.
- `snapshot.py` — immutable redacted metrics snapshot writer, `--verify` self-test.
- `lib/credentials.py` — credential resolver: Windows Credential Manager (keyring, service `loop-agency`) first, then `projects/<slug>/.env` gated behind an `icacls` ACL check that refuses any principal beyond current user + SYSTEM/Administrators. CLI: `--store <alias>` (interactive, how the human hands over secrets), `--check <alias> [--project <slug>]` (never prints values), `--verify` self-test.
- `lib/errors.py` — shared `ConnectorError` (re-exported from `mock_metrics.py` for compat).
- `mock_metrics.py` — synthetic connector for `_demo` only; supports `normal`/`breach`/`fail` scenarios. Never calls a real API.
- `lib/serp_match.py` — Issue #1 (2026-08-16): the shared "is this SERP result ours, and where?" matcher — `registrable_host`, `path_matches` (segment-boundary, so `/massage/` never matches `/massage-therapy-guide/`), `is_own_url` (host equality accepting `www.` and subdomains), and `scan_organic` (one pass counting the true organic index, returning the target-page match and every own-domain hit). Extracted so `dataforseo.py`, `serp_diagnostic.py` and `lib/timeseries.py` cannot drift apart on this question again; `timeseries.py` imports it rather than keeping its own copies. Pure functions, no I/O. `--verify` self-test (32 checks).
- `gsc.py`, `dataforseo.py` — real connectors, dispatched per `spec.inputs` entry by `run_loop.py`'s `_fetch_metrics` since Phase 2 Step 2. GSC is the primary metrics source; DataForSEO enriches matching (keyword, page) rows with `serp_position` and provides local rank / backlinks. Both redact internally and refuse to run without a credential resolver. **`dataforseo.py` was rewritten 2026-08-16 (Issue #1, `CURRENT-WORK.md`; design record `PLAN-SEO-PROGRAM-INTEGRATION.md` P0.1) — it had never worked.** Three simultaneous defects: it POSTed all targets as one array, but the `live/advanced` endpoint executes **one task per request** and rejects the rest (`40000`/`result: null`/`cost: 0`), which `task.get("result") or []` turned into a false "not ranking" — 10 of 12 targets were never queried for months (D4); it matched results by bare URL substring with no domain check, recording competitors' rankings as the client's (D1); and it stored `rank_absolute`, which counts ads/local-pack/PAA blocks, under an "organic rank" label (D5). Now: **one POST per target**, tasks selected by the keyword the API echoes back (never by array index), and every non-`20000` outcome recorded as an explicit error. Each `local_rank` row carries `status: ok | absent | error` — `absent` requires positive evidence of a populated SERP, so a failed call can never read as an absence — plus `matched_domain`, `organic_rank_group`, `organic_results_seen` and `domain_presence`. Non-matching results are **discarded** (no competitor host/URL/title reaches disk — `CURRENT-WORK.md` decision 4). The section carries `schema_version: 2`, `rank_metric: "organic_index"`, and `request_count`/`success_count`/`error_count` (because `run.json` records one `tool_call` per connector, not per HTTP request). `pull_metrics` got the same fix but **fails closed** — any target failure raises — because `dataforseo` is a `critical` connector feeding `search_analytics`, whereas `dataforseo-local-rank` is enrichment and records per-target failures instead. **`domain` is now required** for both DataForSEO inputs (`connector_registry.py` `requires`), with no fallback: a "no domain configured" default would revive D1's substring match.
- `smoke_test.py` — connectors-only smoke test (`python tools/smoke_test.py <project>`): resolves each alias (reports which store answered, never the value), calls each connector live once, prints a redacted summary, includes a bad-alias clean-failure simulation. Writes nothing under `runs/`; takes no lock. `--verify` self-test.
- `run_loop.py` — the run contract engine described above.
- `review_pending.py` — approve/reject/list proposals, resolve a breach.
- `apply.py` — for an approved, opted-in Tier-1 proposal, performs the local `implemented` transition: a read-only `git fetch origin` to confirm the local `main` matches `origin/main` (refuses on drift, Phase 5), then an isolated `git worktree` commit on an unpushed branch. Never pushes, merges, or deploys — those stay Tier 2, human-only (R6). Re-checks approval and tier itself regardless of caller.
- `publish.py` — Phase 6. For an `implemented` proposal with a recorded human approval (`decision.action` of `approve`/`retry`, already written by `review_pending.py`), re-verifies commit ancestry, diffs the commit's own git objects (not the working tree) to confirm exactly one changed file whose blob contains the approved `new_value`, confirms the local branch hasn't moved, checks `.github/workflows/deploy.yml`'s trigger shape at the base commit, validates `origin`'s push URL against the expected `owner/repo`, and hard-refuses unless GitHub's branch-protection API confirms `main` is fully locked down — then pushes the commit to a fully-qualified `seo/<id>` ref, opens a PR via `lib/github_compare.py`'s `create_pull_request()`, and requests auto-merge via `enable_auto_merge()` (GraphQL `enablePullRequestAutoMerge` — no `gh` CLI dependency). Never pushes/merges to `main`/`master` itself; branch protection plus GitHub's own required-review gate is the actual boundary (`PLAN.md` "Where the security boundary actually is"). Token via `credentials.py` alias `<project>-github-token`. Re-acquires the shared run lock like `apply.py`/`review_pending.py`. The PR/auto-merge path is not wired to any project.
- `deploy_health.py` / `deploy_verify.py` — Phase 6a offline-tested health and rollback orchestration. Records new and previous deployment SHAs, uses a non-blocking stabilization window, runs bounded public-site checks, persists redacted results, and models rollback/escalation through an injected redeploy hook. The default hook refuses; no project is wired to deploy or rollback automatically, and `art` remains propose-only/manual-approval-only.
- `watchdog.py` — out-of-band scheduling watchdog (Tier 0, local-only, read-only); checks that each active loop's expected run artifact exists for its cadence window. "Active" is now enforced: it reads `loops_enabled` from `projects/<slug>/project.md` and reports a loop absent from that list as `not-enabled` rather than alerting on it (2026-08-10 — `_demo` is an on-demand fixture and was firing a false alert every single day). This is the only code that reads `loops_enabled`; `run_loop.py` deliberately does not, so a disabled loop stays runnable by hand.
- `scheduled/task.cmd` — the wrapper every Windows Task Scheduler job runs through. Resolves the workspace root from its own path, pins `.venv\Scripts\python.exe`, appends stdout+stderr to `logs/<name>.log` (rotating at 2 MB), and exits with the child's code. Registration commands and the collision/redirection history are in `PHASE3-SCHEDULING.md`.
- `lib/action_types.py` — Phase 7: `IMPLEMENTABLE_ACTIONS`, extracted from `apply.py` into a neutral module both it and `lib/review_protocol.py` import, avoiding a circular import.
- `lib/event_log.py` — Phase 7: append-only, redacted, per-loop event log (`events.jsonl`). Source of truth (event-sourced) for the review subsystem's `status`/`review`/`implementation` projection (`sync_proposal_projection`, `rebuild_review_rounds_from_events`); best-effort observability layered onto the pre-existing state machine elsewhere (`--reconcile`). Tail-corruption is quarantined (`events.jsonl.quarantine`), never deleted; any malformed/schema-invalid line anywhere in the log sets a global `events.jsonl.corrupt_marker` that fails closed for authorization-adjacent reads. `--verify` self-test (26 checks).
- `lib/review_protocol.py` — Phase 7: deterministic, LLM-free evidence-packet building, hash computation (`proposal_content_hash`/`evidence_packet_hash`/`spec_content_hash`), pass-based adjudication (`adjudicate`, selecting by `pass_number` not content hash — a revision that returns to its exact original content must not collide two passes), and `evaluate_auto_implementation_eligibility()` (authorizes tier/`manual_approval_only` from the *current* spec's `allowed_actions` entry, never a proposal's own cached fields). Never calls Codex itself. `--verify` self-test (22 checks).
- `codex_review_proposal.py` — Phase 7: the only tool a Claude session shells out to for review-pipeline state mutation (`--start-review`/`--evidence-packet`/`--record-round`/`--adjudicate`/`--auto-implement`, all `<project> <loop> <id>`). Persists one evidence-packet artifact per pass (`pending/.review-artifacts/<id>-pass<N>.json`, refuses to overwrite). Never calls Codex — see `.claude/skills/codex-seo-review/SKILL.md` for the actual `codex exec` invocation.
- `lib/timeseries.py` / `seo_timeseries.py` — the SEO progress dashboard (`PLAN-SEO-TIMESERIES.md`, approved by Codex at plan-review round 7; transcript `PLAN-REVIEW-LOG-SEO-TIMESERIES.md`). `seo_timeseries.py <project> <loop>` derives a keyword-level time series from every `runs/*/snapshot.json` + `run.json` and writes `projects/<slug>/loops/<loop>/timeseries/{dashboard.html,series.json,gsc_queries.csv}` — **gitignored by path**, 100% rebuildable, never committed (that was a deliberate default; see the plan's Q1). **Read-only and entirely outside the run contract**: it adds no code to `run_loop.py`, no snapshot section, and no scheduled job, so it cannot fail, degrade or delay a run. Two rules dominate it: observations are deduplicated by *measurement timestamp*, never by run (22 snapshots hold only 9 distinct GSC pulls — one point per run would fabricate 13), and no number is drawn that the data cannot support — most consequentially **the dashboard shows no local rank at all**, because 10 of the 12 configured rank targets have never actually been queried (defect D4) and the values that were returned are `rank_absolute` matched by bare URL substring (D1/D5). The page is static HTML + inline SVG with **zero JavaScript** (the Python-only rule covers generated browser JS) and no external references of any kind. `--check` detects staleness; `--verify` self-tests (25 checks; `lib/timeseries.py --verify` adds 27).
- `gsc_daily.py` — Phase B of the same plan: one extra **read-only** Search Analytics call with `dimensions: ["date"]`, which `gsc.pull_metrics()` cannot serve (it hard-maps `keys[0] → keyword`), so this tool owns its own parser and *imports* `gsc.py`'s auth/redaction/error helpers without modifying it. Gives true non-overlapping ISO weeks, which the loop's trailing-28-day window structurally cannot. Its refresh window starts at wherever `gsc_daily.jsonl` actually ends (clamped to retention), so any invocation cadence yields a complete series; `gsc_daily.meta.json` carries a sha256 over the jsonl's bytes and the dashboard refuses to render a weekly chart unless they match. **Manual/on-demand — nothing schedules it.** `--verify` self-tests (14 checks, fully offline). Two things running it found, both recorded in `PLAN-SEO-TIMESERIES.md` Part 1b: GSC has **no data before 2026-07-15** for this property (so there is no pre-loop baseline), and the per-query pull the loop optimises against sees only **23 of the site's 72 clicks** in the same window — Google withholds rare queries, so every traffic-volume conclusion in `PLAN-SEO-PROGRAM-INTEGRATION.md` rests on roughly a third of the real number.
- `draft_link.py` — Phase 7: `draft_copy.py`'s counterpart for `internal-link-addition` (`source_page`/`destination_page`/`anchor_text`, validated against `priority_pages`/`noindex_destination_pages`). Still never implements an HTML mutation. `--verify` self-test (17 checks).
- **GBP Posts plan (Phases 1–4 shipped 2026-08-31, built for `art/gbp` while the operator was away — see `CURRENT-WORK.md` "GBP Posts loop — Phases 1–4 built" and `RISK-REGISTER.md` R19 for the full record; `projects/art/loops/gbp/spec.md` for what the loop does/doesn't do yet).** No publish path exists — nothing in this workspace can create or edit a live GBP Post.
  - `dataforseo_maps.py` / `dataforseo_local_pack.py` — Phase 1. Maps-rank and local-pack-presence connectors (`connector_registry.py` keys `dataforseo-maps-rank`/`dataforseo-local-pack`, both `critical: False`), matching this client's own listing ONLY by a verified `place_id`/`cid` (never name/domain — standing decision 4 and the D1 lesson both apply). Not wired into any spec's `inputs` yet — needs a human-verified `place_id`/`cid` per location first (Phase 0). `--verify` self-tests (14 and 23 checks).
  - `lib/gbp_constraints.py` — Phase 4. Parses a small, versioned, hashed projection of two sources governing what a GBP Post may say: MMC's `registry/clients/<slug>.yaml` (trademark/service-area/closed-location facts, each dated) and `gbp-profiles.md`'s wording rules (via a new confirmation sidecar, `gbp-profile-confirmations.yaml`, since the prose itself carries no per-fact dates). Any field either source marks unconfirmed, or confirmed >180 days ago, is never asserted as safe; a genuine disagreement between the two is a hard failure. Hardened via four rounds of Codex review — see the module's own inline history before touching it, especially `PARSER_VERSION` (currently 2) and `SINGLE_VALUED_FACT_KINDS`'s narrow scoping. `--verify` self-test (38 checks, re-verified against the real cross-repo files).
  - `draft_gbp_post.py` — Phase 4. `draft_copy.py`'s counterpart for `gbp-post-draft` proposals: validates a STANDARD-type Post (EVENT/OFFER refused outright) against Google's public API docs (character limit, CTA enum — not independently verified against a live response, no credential exists yet), a same-origin-only link allowlist, a deterministic PHI/medical-claims/urgency lint, and `lib/gbp_constraints.py`'s cross-repo gate. This lint is an explicitly-acknowledged backstop, not an exhaustive classifier — the human-supervised drafting session is the actual first line of defense. `--verify` self-test (50 checks).
  - `run_loop.py` additions — `_pick_gbp_actions` (Phase 3): drafts a `gbp-post-draft` proposal only from a `known-gaps.yaml` entry explicitly routed `content_gap_evidence`, never from a rank/pack/technical-health signal alone; its rationale is templated exclusively from structured fields, never a gap's free-text `summary` (which could itself smuggle a rank-fix claim). The pre-existing SEO selector/evaluator (`_pick_new_actions`/`_evaluate_prior_experiments`) are now structurally gated on `spec["loop"] == "seo"`, not just on which connectors happen to be configured — closes a stale-`search_analytics`-carry-forward loophole that would otherwise let the SEO-shaped selector fire for a non-SEO loop.
- Scripts import each other via flat imports (e.g. `from lib.redact import redact_deep`) and are invoked as direct file paths (`python tools/run_loop.py ...`), not as an installed package — run them from the workspace root.

## Skills (`.claude/skills/`)

`/intake-project`, `/run-loop`, `/review-pending` — see each `SKILL.md` for exact steps. None of them bypass the tools above; they orchestrate and summarize for the human. `/codex-seo-review` (Phase 7, dormant by default — see "Phase status" above) runs the two-round Codex adversarial review + eligibility-gated auto-implementation path; it never runs for a project whose spec hasn't explicitly opted into all three gates, and never runs for `art`.

## Secrets & tenant isolation

Windows Credential Manager is the default secret store; per-project `.env` is an explicit fallback only, requires restricted ACLs, and the runner must refuse to start against an over-broad `.env`. Repo files store only opaque aliases, never raw values. A run may only read/write inside `projects/<slug>/` and its declared repo, checked via resolved canonical paths (`tools/lib/paths.py`) — never naive prefix matching.

## Debugging

Never rely on model memory to reconstruct what a run did — `runs/<run-id>/run.json` is the observability contract. If something looks wrong, read that file (and `snapshot.json`, `report.md`) before speculating.

## MMC (shared brain)

Before cross-system work (anything touching another of Nate's four systems, or client facts shared across them), read `D:\Dev\MMC\MMC.md` first. `@import` auto-load was tested and does not reliably resolve in headless (`claude -p`) sessions — this is a manual-read instruction, not an automatic one.
